"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class KeldonTwilight extends UnimplementedCard {
  constructor(game) {
    super(game, "Keldon Twilight", "Planeshift", "PLS");
  }
}

module.exports = KeldonTwilight;
