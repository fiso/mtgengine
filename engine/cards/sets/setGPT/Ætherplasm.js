"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Ætherplasm extends UnimplementedCard {
  constructor(game) {
    super(game, "Ætherplasm", "Guildpact", "GPT");
  }
}

module.exports = Ætherplasm;
