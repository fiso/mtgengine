"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Glamerdye extends UnimplementedCard {
  constructor(game) {
    super(game, "Glamerdye", "Eventide", "EVE");
  }
}

module.exports = Glamerdye;
