"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UmbraStalker extends UnimplementedCard {
  constructor(game) {
    super(game, "Umbra Stalker", "Eventide", "EVE");
  }
}

module.exports = UmbraStalker;
