"use strict";
const Card = require("../../objects/Card");
const Constants = require ("../../Constants");

class Primalcrux extends Card {
  constructor(game) {
    super(game, "Primalcrux", "Eventide", "EVE");
  }
}

module.exports = Primalcrux;
