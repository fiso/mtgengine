"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MerrowBonegnawer extends UnimplementedCard {
  constructor(game) {
    super(game, "Merrow Bonegnawer", "Eventide", "EVE");
  }
}

module.exports = MerrowBonegnawer;
