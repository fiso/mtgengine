"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MerrowLevitator extends UnimplementedCard {
  constructor(game) {
    super(game, "Merrow Levitator", "Eventide", "EVE");
  }
}

module.exports = MerrowLevitator;
