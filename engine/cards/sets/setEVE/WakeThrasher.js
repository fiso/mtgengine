"use strict";
const Card = require("../../objects/Card");
const Constants = require ("../../Constants");

class WakeThrasher extends Card {
  constructor(game) {
    super(game, "Wake Thrasher", "Eventide", "EVE");
  }
}

module.exports = WakeThrasher;
