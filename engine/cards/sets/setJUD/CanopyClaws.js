"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CanopyClaws extends UnimplementedCard {
  constructor(game) {
    super(game, "Canopy Claws", "Judgment", "JUD");
  }
}

module.exports = CanopyClaws;
