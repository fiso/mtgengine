"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WelkinHawk extends UnimplementedCard {
  constructor(game) {
    super(game, "Welkin Hawk", "Exodus", "EXO");
  }
}

module.exports = WelkinHawk;
