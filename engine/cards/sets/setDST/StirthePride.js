"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class StirthePride extends UnimplementedCard {
  constructor(game) {
    super(game, "Stir the Pride", "Darksteel", "DST");
  }
}

module.exports = StirthePride;
