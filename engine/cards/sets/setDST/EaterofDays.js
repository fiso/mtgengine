"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EaterofDays extends UnimplementedCard {
  constructor(game) {
    super(game, "Eater of Days", "Darksteel", "DST");
  }
}

module.exports = EaterofDays;
