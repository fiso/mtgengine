"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UktabiKong extends UnimplementedCard {
  constructor(game) {
    super(game, "Uktabi Kong", "Unhinged", "UNH");
  }
}

module.exports = UktabiKong;
