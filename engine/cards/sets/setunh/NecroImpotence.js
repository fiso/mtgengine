"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class NecroImpotence extends UnimplementedCard {
  constructor(game) {
    super(game, "Necro-Impotence", "Unhinged", "UNH");
  }
}

module.exports = NecroImpotence;
