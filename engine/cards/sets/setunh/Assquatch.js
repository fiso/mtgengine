"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Assquatch extends UnimplementedCard {
  constructor(game) {
    super(game, "Assquatch", "Unhinged", "UNH");
  }
}

module.exports = Assquatch;
