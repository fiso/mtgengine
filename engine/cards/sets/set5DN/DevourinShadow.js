"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DevourinShadow extends UnimplementedCard {
  constructor(game) {
    super(game, "Devour in Shadow", "Fifth Dawn", "5DN");
  }
}

module.exports = DevourinShadow;
