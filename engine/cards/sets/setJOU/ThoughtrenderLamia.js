"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ThoughtrenderLamia extends UnimplementedCard {
  constructor(game) {
    super(game, "Thoughtrender Lamia", "Journey into Nyx", "JOU");
  }
}

module.exports = ThoughtrenderLamia;
