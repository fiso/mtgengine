"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DismissintoDream extends UnimplementedCard {
  constructor(game) {
    super(game, "Dismiss into Dream", "Magic 2014 Core Set", "M14");
  }
}

module.exports = DismissintoDream;
