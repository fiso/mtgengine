"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Crocanura extends UnimplementedCard {
  constructor(game) {
    super(game, "Crocanura", "Gatecrash", "GTC");
  }
}

module.exports = Crocanura;
