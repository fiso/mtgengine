"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GiltLeafAmbush extends UnimplementedCard {
  constructor(game) {
    super(game, "Gilt-Leaf Ambush", "Lorwyn", "LRW");
  }
}

module.exports = GiltLeafAmbush;
