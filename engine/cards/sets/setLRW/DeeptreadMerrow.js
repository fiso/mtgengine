"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeeptreadMerrow extends UnimplementedCard {
  constructor(game) {
    super(game, "Deeptread Merrow", "Lorwyn", "LRW");
  }
}

module.exports = DeeptreadMerrow;
