"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GiltLeafPalace extends UnimplementedCard {
  constructor(game) {
    super(game, "Gilt-Leaf Palace", "Lorwyn", "LRW");
  }
}

module.exports = GiltLeafPalace;
