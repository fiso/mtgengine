"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HideousVisage extends UnimplementedCard {
  constructor(game) {
    super(game, "Hideous Visage", "Magic 2012", "M12");
  }
}

module.exports = HideousVisage;
