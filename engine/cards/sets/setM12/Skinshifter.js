"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Skinshifter extends UnimplementedCard {
  constructor(game) {
    super(game, "Skinshifter", "Magic 2012", "M12");
  }
}

module.exports = Skinshifter;
