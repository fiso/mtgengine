"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RangersPath extends UnimplementedCard {
  constructor(game) {
    super(game, "Ranger's Path", "Magic 2013", "M13");
  }
}

module.exports = RangersPath;
