"use strict";
const Card = require("../../objects/Card");
const Constants = require ("../../Constants");

class LightningHounds extends Card {
  constructor(game) {
    super(game, "Lightning Hounds", "Media Inserts", "pMEI");
  }
}

module.exports = LightningHounds;
