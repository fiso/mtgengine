"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FieryImpulse extends UnimplementedCard {
  constructor(game) {
    super(game, "Fiery Impulse", "Magic Origins", "ORI");
  }
}

module.exports = FieryImpulse;
