"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Counterflux extends UnimplementedCard {
  constructor(game) {
    super(game, "Counterflux", "Commander 2015", "C15");
  }
}

module.exports = Counterflux;
