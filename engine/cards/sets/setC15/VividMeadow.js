"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VividMeadow extends UnimplementedCard {
  constructor(game) {
    super(game, "Vivid Meadow", "Commander 2015", "C15");
  }
}

module.exports = VividMeadow;
