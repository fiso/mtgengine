"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GrotagThrasher extends UnimplementedCard {
  constructor(game) {
    super(game, "Grotag Thrasher", "Worldwake", "WWK");
  }
}

module.exports = GrotagThrasher;
