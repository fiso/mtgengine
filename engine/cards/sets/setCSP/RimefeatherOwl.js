"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RimefeatherOwl extends UnimplementedCard {
  constructor(game) {
    super(game, "Rimefeather Owl", "Coldsnap", "CSP");
  }
}

module.exports = RimefeatherOwl;
