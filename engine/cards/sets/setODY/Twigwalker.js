"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Twigwalker extends UnimplementedCard {
  constructor(game) {
    super(game, "Twigwalker", "Odyssey", "ODY");
  }
}

module.exports = Twigwalker;
