"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DreamSalvage extends UnimplementedCard {
  constructor(game) {
    super(game, "Dream Salvage", "Shadowmoor", "SHM");
  }
}

module.exports = DreamSalvage;
