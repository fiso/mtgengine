"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MadblindMountain extends UnimplementedCard {
  constructor(game) {
    super(game, "Madblind Mountain", "Shadowmoor", "SHM");
  }
}

module.exports = MadblindMountain;
