"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RattleblazeScarecrow extends UnimplementedCard {
  constructor(game) {
    super(game, "Rattleblaze Scarecrow", "Shadowmoor", "SHM");
  }
}

module.exports = RattleblazeScarecrow;
