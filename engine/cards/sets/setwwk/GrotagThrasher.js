"use strict";
const Card = require("../../objects/Card");
const Constants = require ("../../Constants");

class GrotagThrasher extends Card {
  constructor(game) {
    super(game, "Grotag Thrasher", "Worldwake", "WWK");
  }
}

module.exports = GrotagThrasher;
