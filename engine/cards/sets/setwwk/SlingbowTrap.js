"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SlingbowTrap extends UnimplementedCard {
  constructor(game) {
    super(game, "Slingbow Trap", "Worldwake", "WWK");
  }
}

module.exports = SlingbowTrap;
