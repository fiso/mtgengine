"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MindbenderSpores extends UnimplementedCard {
  constructor(game) {
    super(game, "Mindbender Spores", "Mirage", "MIR");
  }
}

module.exports = MindbenderSpores;
