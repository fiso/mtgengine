"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LightningReflexes extends UnimplementedCard {
  constructor(game) {
    super(game, "Lightning Reflexes", "Mirage", "MIR");
  }
}

module.exports = LightningReflexes;
