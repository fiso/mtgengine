"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EkunduCyclops extends UnimplementedCard {
  constructor(game) {
    super(game, "Ekundu Cyclops", "Mirage", "MIR");
  }
}

module.exports = EkunduCyclops;
