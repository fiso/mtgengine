"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DrainpipeVermin extends UnimplementedCard {
  constructor(game) {
    super(game, "Drainpipe Vermin", "Return to Ravnica", "RTR");
  }
}

module.exports = DrainpipeVermin;
