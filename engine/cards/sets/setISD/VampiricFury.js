"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VampiricFury extends UnimplementedCard {
  constructor(game) {
    super(game, "Vampiric Fury", "Innistrad", "ISD");
  }
}

module.exports = VampiricFury;
