"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HomicidalBrute extends UnimplementedCard {
  constructor(game) {
    super(game, "Homicidal Brute", "Innistrad", "ISD");
  }
}

module.exports = HomicidalBrute;
