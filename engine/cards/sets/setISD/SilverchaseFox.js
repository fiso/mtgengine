"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SilverchaseFox extends UnimplementedCard {
  constructor(game) {
    super(game, "Silverchase Fox", "Innistrad", "ISD");
  }
}

module.exports = SilverchaseFox;
