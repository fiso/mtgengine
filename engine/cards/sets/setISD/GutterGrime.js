"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GutterGrime extends UnimplementedCard {
  constructor(game) {
    super(game, "Gutter Grime", "Innistrad", "ISD");
  }
}

module.exports = GutterGrime;
