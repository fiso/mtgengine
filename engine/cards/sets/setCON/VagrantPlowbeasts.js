"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VagrantPlowbeasts extends UnimplementedCard {
  constructor(game) {
    super(game, "Vagrant Plowbeasts", "Conflux", "CON");
  }
}

module.exports = VagrantPlowbeasts;
