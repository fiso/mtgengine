"use strict";
const Card = require("../../objects/Card");
const Constants = require ("../../Constants");

class SporeBurst extends Card {
  constructor(game) {
    super(game, "Spore Burst", "Conflux", "CON");
  }
}

module.exports = SporeBurst;
