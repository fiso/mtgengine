"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Meglonoth extends UnimplementedCard {
  constructor(game) {
    super(game, "Meglonoth", "Conflux", "CON");
  }
}

module.exports = Meglonoth;
