"use strict";
const Card = require("../../objects/Card");
const Constants = require ("../../Constants");

class SedraxisAlchemist extends Card {
  constructor(game) {
    super(game, "Sedraxis Alchemist", "Conflux", "CON");
  }
}

module.exports = SedraxisAlchemist;
