"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ToxicIguanar extends UnimplementedCard {
  constructor(game) {
    super(game, "Toxic Iguanar", "Conflux", "CON");
  }
}

module.exports = ToxicIguanar;
