"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AltarofShadows extends UnimplementedCard {
  constructor(game) {
    super(game, "Altar of Shadows", "Mirrodin", "MRD");
  }
}

module.exports = AltarofShadows;
