"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SigardaHostofHerons extends UnimplementedCard {
  constructor(game) {
    super(game, "Sigarda, Host of Herons", "Avacyn Restored", "AVR");
  }
}

module.exports = SigardaHostofHerons;
