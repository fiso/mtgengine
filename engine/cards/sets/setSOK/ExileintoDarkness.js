"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ExileintoDarkness extends UnimplementedCard {
  constructor(game) {
    super(game, "Exile into Darkness", "Saviors of Kamigawa", "SOK");
  }
}

module.exports = ExileintoDarkness;
