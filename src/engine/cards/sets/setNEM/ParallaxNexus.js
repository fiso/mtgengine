"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ParallaxNexus extends UnimplementedCard {
  constructor (game) {
    super(game, "Parallax Nexus", "Nemesis", "NEM");
  }
}

module.exports = ParallaxNexus;
