"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TezzerettheSchemerEmblem extends UnimplementedCard {
  constructor (game) {
    super(game, "Tezzeret the Schemer Emblem", "Aether Revolt Tokens", "TAER");
  }
}

module.exports = TezzerettheSchemerEmblem;
