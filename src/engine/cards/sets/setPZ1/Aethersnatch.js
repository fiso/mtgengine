"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Aethersnatch extends UnimplementedCard {
  constructor (game) {
    super(game, "Aethersnatch", "Legendary Cube", "PZ1");
  }
}

module.exports = Aethersnatch;
