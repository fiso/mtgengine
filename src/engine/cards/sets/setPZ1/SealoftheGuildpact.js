"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SealoftheGuildpact extends UnimplementedCard {
  constructor (game) {
    super(game, "Seal of the Guildpact", "Legendary Cube", "PZ1");
  }
}

module.exports = SealoftheGuildpact;
