"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Cultivate extends UnimplementedCard {
  constructor (game) {
    super(game, "Cultivate", "Commander 2018", "C18");
  }
}

module.exports = Cultivate;
