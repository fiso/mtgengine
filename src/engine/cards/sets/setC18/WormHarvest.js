"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WormHarvest extends UnimplementedCard {
  constructor (game) {
    super(game, "Worm Harvest", "Commander 2018", "C18");
  }
}

module.exports = WormHarvest;
