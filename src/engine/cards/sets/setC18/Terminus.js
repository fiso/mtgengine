"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Terminus extends UnimplementedCard {
  constructor (game) {
    super(game, "Terminus", "Commander 2018", "C18");
  }
}

module.exports = Terminus;
