"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HalimarDepths extends UnimplementedCard {
  constructor (game) {
    super(game, "Halimar Depths", "Commander 2018", "C18");
  }
}

module.exports = HalimarDepths;
