"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UtterEnd extends UnimplementedCard {
  constructor (game) {
    super(game, "Utter End", "Commander 2018", "C18");
  }
}

module.exports = UtterEnd;
