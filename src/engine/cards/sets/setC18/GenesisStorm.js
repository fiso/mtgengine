"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GenesisStorm extends UnimplementedCard {
  constructor (game) {
    super(game, "Genesis Storm", "Commander 2018", "C18");
  }
}

module.exports = GenesisStorm;
