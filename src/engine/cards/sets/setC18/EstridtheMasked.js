"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EstridtheMasked extends UnimplementedCard {
  constructor (game) {
    super(game, "Estrid, the Masked", "Commander 2018", "C18");
  }
}

module.exports = EstridtheMasked;
