"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RushwoodDryad extends UnimplementedCard {
  constructor (game) {
    super(game, "Rushwood Dryad", "Tenth Edition", "10E");
  }
}

module.exports = RushwoodDryad;
