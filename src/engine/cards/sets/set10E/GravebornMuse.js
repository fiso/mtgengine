"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GravebornMuse extends UnimplementedCard {
  constructor (game) {
    super(game, "Graveborn Muse", "Tenth Edition", "10E");
  }
}

module.exports = GravebornMuse;
