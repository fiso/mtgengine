"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Cytoshape extends UnimplementedCard {
  constructor (game) {
    super(game, "Cytoshape", "Dissension", "DIS");
  }
}

module.exports = Cytoshape;
