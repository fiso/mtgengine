"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ArchfiendofDespair extends UnimplementedCard {
  constructor (game) {
    super(game, "Archfiend of Despair", "Battlebond", "BBD");
  }
}

module.exports = ArchfiendofDespair;
