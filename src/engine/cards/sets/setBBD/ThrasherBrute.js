"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ThrasherBrute extends UnimplementedCard {
  constructor (game) {
    super(game, "Thrasher Brute", "Battlebond", "BBD");
  }
}

module.exports = ThrasherBrute;
