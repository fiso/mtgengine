"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ConsulateSkygate extends UnimplementedCard {
  constructor (game) {
    super(game, "Consulate Skygate", "Battlebond", "BBD");
  }
}

module.exports = ConsulateSkygate;
