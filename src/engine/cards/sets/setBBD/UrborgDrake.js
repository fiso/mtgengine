"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UrborgDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Urborg Drake", "Battlebond", "BBD");
  }
}

module.exports = UrborgDrake;
