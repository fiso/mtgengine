"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Hydrolash extends UnimplementedCard {
  constructor (game) {
    super(game, "Hydrolash", "Magic Origins", "ORI");
  }
}

module.exports = Hydrolash;
