"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ValeronWardens extends UnimplementedCard {
  constructor (game) {
    super(game, "Valeron Wardens", "Magic Origins", "ORI");
  }
}

module.exports = ValeronWardens;
