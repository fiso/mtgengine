"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Prickleboar extends UnimplementedCard {
  constructor (game) {
    super(game, "Prickleboar", "Magic Origins", "ORI");
  }
}

module.exports = Prickleboar;
