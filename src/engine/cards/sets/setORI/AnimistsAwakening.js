"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AnimistsAwakening extends UnimplementedCard {
  constructor (game) {
    super(game, "Animist's Awakening", "Magic Origins", "ORI");
  }
}

module.exports = AnimistsAwakening;
