"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SoldeviAdnate extends UnimplementedCard {
  constructor (game) {
    super(game, "Soldevi Adnate", "Alliances", "ALL");
  }
}

module.exports = SoldeviAdnate;
