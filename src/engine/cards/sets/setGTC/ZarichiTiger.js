"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ZarichiTiger extends UnimplementedCard {
  constructor (game) {
    super(game, "Zarichi Tiger", "Gatecrash", "GTC");
  }
}

module.exports = ZarichiTiger;
