"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SapphireDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Sapphire Drake", "Gatecrash", "GTC");
  }
}

module.exports = SapphireDrake;
