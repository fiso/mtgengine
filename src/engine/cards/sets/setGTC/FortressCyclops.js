"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FortressCyclops extends UnimplementedCard {
  constructor (game) {
    super(game, "Fortress Cyclops", "Gatecrash", "GTC");
  }
}

module.exports = FortressCyclops;
