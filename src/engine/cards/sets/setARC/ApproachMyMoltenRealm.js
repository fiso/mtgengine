"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ApproachMyMoltenRealm extends UnimplementedCard {
  constructor (game) {
    super(game, "Approach My Molten Realm", "Archenemy", "ARC");
  }
}

module.exports = ApproachMyMoltenRealm;
