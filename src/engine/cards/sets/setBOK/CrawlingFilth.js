"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CrawlingFilth extends UnimplementedCard {
  constructor (game) {
    super(game, "Crawling Filth", "Betrayers of Kamigawa", "BOK");
  }
}

module.exports = CrawlingFilth;
