"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MagefireWings extends UnimplementedCard {
  constructor (game) {
    super(game, "Magefire Wings", "Alara Reborn", "ARB");
  }
}

module.exports = MagefireWings;
