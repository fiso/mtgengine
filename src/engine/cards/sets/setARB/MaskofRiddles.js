"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MaskofRiddles extends UnimplementedCard {
  constructor (game) {
    super(game, "Mask of Riddles", "Alara Reborn", "ARB");
  }
}

module.exports = MaskofRiddles;
