"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DefilingTears extends UnimplementedCard {
  constructor (game) {
    super(game, "Defiling Tears", "Invasion", "INV");
  }
}

module.exports = DefilingTears;
