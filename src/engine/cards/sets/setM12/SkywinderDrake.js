"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SkywinderDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Skywinder Drake", "Magic 2012", "M12");
  }
}

module.exports = SkywinderDrake;
