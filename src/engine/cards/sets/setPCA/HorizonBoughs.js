"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HorizonBoughs extends UnimplementedCard {
  constructor (game) {
    super(game, "Horizon Boughs", "Planechase Anthology", "PCA");
  }
}

module.exports = HorizonBoughs;
