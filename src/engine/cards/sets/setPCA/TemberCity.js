"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TemberCity extends UnimplementedCard {
  constructor (game) {
    super(game, "Tember City", "Planechase Anthology", "PCA");
  }
}

module.exports = TemberCity;
