"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BoarUmbra extends UnimplementedCard {
  constructor (game) {
    super(game, "Boar Umbra", "Planechase Anthology", "PCA");
  }
}

module.exports = BoarUmbra;
