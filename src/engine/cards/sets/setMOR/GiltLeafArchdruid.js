"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GiltLeafArchdruid extends UnimplementedCard {
  constructor (game) {
    super(game, "Gilt-Leaf Archdruid", "Morningtide", "MOR");
  }
}

module.exports = GiltLeafArchdruid;
