"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GelidShackles extends UnimplementedCard {
  constructor (game) {
    super(game, "Gelid Shackles", "Coldsnap", "CSP");
  }
}

module.exports = GelidShackles;
