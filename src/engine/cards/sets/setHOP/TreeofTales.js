"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TreeofTales extends UnimplementedCard {
  constructor (game) {
    super(game, "Tree of Tales", "Planechase", "HOP");
  }
}

module.exports = TreeofTales;
