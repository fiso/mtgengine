"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BullCerodon extends UnimplementedCard {
  constructor (game) {
    super(game, "Bull Cerodon", "Planechase", "HOP");
  }
}

module.exports = BullCerodon;
