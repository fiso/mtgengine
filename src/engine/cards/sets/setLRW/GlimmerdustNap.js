"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GlimmerdustNap extends UnimplementedCard {
  constructor (game) {
    super(game, "Glimmerdust Nap", "Lorwyn", "LRW");
  }
}

module.exports = GlimmerdustNap;
