"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SecludedGlen extends UnimplementedCard {
  constructor (game) {
    super(game, "Secluded Glen", "Lorwyn", "LRW");
  }
}

module.exports = SecludedGlen;
