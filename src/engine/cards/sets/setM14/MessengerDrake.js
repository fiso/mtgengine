"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MessengerDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Messenger Drake", "Magic 2014", "M14");
  }
}

module.exports = MessengerDrake;
