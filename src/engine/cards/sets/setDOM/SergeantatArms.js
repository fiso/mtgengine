"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SergeantatArms extends UnimplementedCard {
  constructor (game) {
    super(game, "Sergeant-at-Arms", "Dominaria", "DOM");
  }
}

module.exports = SergeantatArms;
