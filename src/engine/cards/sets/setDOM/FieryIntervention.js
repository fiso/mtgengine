"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FieryIntervention extends UnimplementedCard {
  constructor (game) {
    super(game, "Fiery Intervention", "Dominaria", "DOM");
  }
}

module.exports = FieryIntervention;
