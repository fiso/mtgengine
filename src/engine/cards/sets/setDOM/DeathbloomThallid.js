"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeathbloomThallid extends UnimplementedCard {
  constructor (game) {
    super(game, "Deathbloom Thallid", "Dominaria", "DOM");
  }
}

module.exports = DeathbloomThallid;
