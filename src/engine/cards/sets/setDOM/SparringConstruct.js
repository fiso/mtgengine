"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SparringConstruct extends UnimplementedCard {
  constructor (game) {
    super(game, "Sparring Construct", "Dominaria", "DOM");
  }
}

module.exports = SparringConstruct;
