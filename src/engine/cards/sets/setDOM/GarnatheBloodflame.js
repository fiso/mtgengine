"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GarnatheBloodflame extends UnimplementedCard {
  constructor (game) {
    super(game, "Garna, the Bloodflame", "Dominaria", "DOM");
  }
}

module.exports = GarnatheBloodflame;
