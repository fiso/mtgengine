"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class JhoiraWeatherlightCaptain extends UnimplementedCard {
  constructor (game) {
    super(game, "Jhoira, Weatherlight Captain", "Dominaria", "DOM");
  }
}

module.exports = JhoiraWeatherlightCaptain;
