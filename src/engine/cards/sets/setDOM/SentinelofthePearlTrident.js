"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SentinelofthePearlTrident extends UnimplementedCard {
  constructor (game) {
    super(game, "Sentinel of the Pearl Trident", "Dominaria", "DOM");
  }
}

module.exports = SentinelofthePearlTrident;
