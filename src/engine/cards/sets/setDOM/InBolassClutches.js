"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class InBolassClutches extends UnimplementedCard {
  constructor (game) {
    super(game, "In Bolas's Clutches", "Dominaria", "DOM");
  }
}

module.exports = InBolassClutches;
