"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FirefistAdept extends UnimplementedCard {
  constructor (game) {
    super(game, "Firefist Adept", "Dominaria", "DOM");
  }
}

module.exports = FirefistAdept;
