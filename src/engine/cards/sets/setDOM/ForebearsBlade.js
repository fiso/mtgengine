"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ForebearsBlade extends UnimplementedCard {
  constructor (game) {
    super(game, "Forebear's Blade", "Dominaria", "DOM");
  }
}

module.exports = ForebearsBlade;
