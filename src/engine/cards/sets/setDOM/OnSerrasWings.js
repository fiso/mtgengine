"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class OnSerrasWings extends UnimplementedCard {
  constructor (game) {
    super(game, "On Serra's Wings", "Dominaria", "DOM");
  }
}

module.exports = OnSerrasWings;
