"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ThranTemporalGateway extends UnimplementedCard {
  constructor (game) {
    super(game, "Thran Temporal Gateway", "Dominaria", "DOM");
  }
}

module.exports = ThranTemporalGateway;
