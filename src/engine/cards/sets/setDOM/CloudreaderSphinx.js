"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CloudreaderSphinx extends UnimplementedCard {
  constructor (game) {
    super(game, "Cloudreader Sphinx", "Dominaria", "DOM");
  }
}

module.exports = CloudreaderSphinx;
