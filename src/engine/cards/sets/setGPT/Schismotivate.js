"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Schismotivate extends UnimplementedCard {
  constructor (game) {
    super(game, "Schismotivate", "Guildpact", "GPT");
  }
}

module.exports = Schismotivate;
