"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Runeboggle extends UnimplementedCard {
  constructor (game) {
    super(game, "Runeboggle", "Guildpact", "GPT");
  }
}

module.exports = Runeboggle;
