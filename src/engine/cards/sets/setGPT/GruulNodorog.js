"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GruulNodorog extends UnimplementedCard {
  constructor (game) {
    super(game, "Gruul Nodorog", "Guildpact", "GPT");
  }
}

module.exports = GruulNodorog;
