"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DrownedRusalka extends UnimplementedCard {
  constructor (game) {
    super(game, "Drowned Rusalka", "Guildpact", "GPT");
  }
}

module.exports = DrownedRusalka;
