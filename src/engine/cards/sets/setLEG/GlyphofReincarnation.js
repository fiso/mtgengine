"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GlyphofReincarnation extends UnimplementedCard {
  constructor (game) {
    super(game, "Glyph of Reincarnation", "Legends", "LEG");
  }
}

module.exports = GlyphofReincarnation;
