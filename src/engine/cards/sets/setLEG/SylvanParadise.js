"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SylvanParadise extends UnimplementedCard {
  constructor (game) {
    super(game, "Sylvan Paradise", "Legends", "LEG");
  }
}

module.exports = SylvanParadise;
