"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Darkpact extends UnimplementedCard {
  constructor (game) {
    super(game, "Darkpact", "Summer Magic / Edgar", "SUM");
  }
}

module.exports = Darkpact;
