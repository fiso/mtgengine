"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RamunapRuins extends UnimplementedCard {
  constructor (game) {
    super(game, "Ramunap Ruins", "Hour of Devastation", "HOU");
  }
}

module.exports = RamunapRuins;
