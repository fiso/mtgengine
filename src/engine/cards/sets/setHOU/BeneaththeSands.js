"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BeneaththeSands extends UnimplementedCard {
  constructor (game) {
    super(game, "Beneath the Sands", "Hour of Devastation", "HOU");
  }
}

module.exports = BeneaththeSands;
