"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class IfnirDeadlands extends UnimplementedCard {
  constructor (game) {
    super(game, "Ifnir Deadlands", "Hour of Devastation", "HOU");
  }
}

module.exports = IfnirDeadlands;
