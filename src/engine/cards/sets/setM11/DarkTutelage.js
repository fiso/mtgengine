"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DarkTutelage extends UnimplementedCard {
  constructor (game) {
    super(game, "Dark Tutelage", "Magic 2011", "M11");
  }
}

module.exports = DarkTutelage;
