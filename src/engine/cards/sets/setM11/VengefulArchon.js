"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VengefulArchon extends UnimplementedCard {
  constructor (game) {
    super(game, "Vengeful Archon", "Magic 2011", "M11");
  }
}

module.exports = VengefulArchon;
