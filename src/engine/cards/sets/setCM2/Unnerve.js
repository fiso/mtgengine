"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Unnerve extends UnimplementedCard {
  constructor (game) {
    super(game, "Unnerve", "Commander Anthology Volume II", "CM2");
  }
}

module.exports = Unnerve;
