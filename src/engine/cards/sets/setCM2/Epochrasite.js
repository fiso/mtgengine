"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Epochrasite extends UnimplementedCard {
  constructor (game) {
    super(game, "Epochrasite", "Commander Anthology Volume II", "CM2");
  }
}

module.exports = Epochrasite;
