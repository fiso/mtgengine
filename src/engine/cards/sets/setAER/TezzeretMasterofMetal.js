"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TezzeretMasterofMetal extends UnimplementedCard {
  constructor (game) {
    super(game, "Tezzeret, Master of Metal", "Aether Revolt", "AER");
  }
}

module.exports = TezzeretMasterofMetal;
