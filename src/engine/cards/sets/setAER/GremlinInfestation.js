"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GremlinInfestation extends UnimplementedCard {
  constructor (game) {
    super(game, "Gremlin Infestation", "Aether Revolt", "AER");
  }
}

module.exports = GremlinInfestation;
