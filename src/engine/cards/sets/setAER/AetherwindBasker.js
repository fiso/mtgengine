"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AetherwindBasker extends UnimplementedCard {
  constructor (game) {
    super(game, "Aetherwind Basker", "Aether Revolt", "AER");
  }
}

module.exports = AetherwindBasker;
