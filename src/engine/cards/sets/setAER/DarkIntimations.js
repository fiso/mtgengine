"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DarkIntimations extends UnimplementedCard {
  constructor (game) {
    super(game, "Dark Intimations", "Aether Revolt", "AER");
  }
}

module.exports = DarkIntimations;
