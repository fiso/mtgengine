"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RenegadesGetaway extends UnimplementedCard {
  constructor (game) {
    super(game, "Renegade's Getaway", "Aether Revolt", "AER");
  }
}

module.exports = RenegadesGetaway;
