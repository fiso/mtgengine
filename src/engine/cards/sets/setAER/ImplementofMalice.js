"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ImplementofMalice extends UnimplementedCard {
  constructor (game) {
    super(game, "Implement of Malice", "Aether Revolt", "AER");
  }
}

module.exports = ImplementofMalice;
