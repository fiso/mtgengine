"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EfficientConstruction extends UnimplementedCard {
  constructor (game) {
    super(game, "Efficient Construction", "Aether Revolt", "AER");
  }
}

module.exports = EfficientConstruction;
