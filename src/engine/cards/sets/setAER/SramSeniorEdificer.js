"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SramSeniorEdificer extends UnimplementedCard {
  constructor (game) {
    super(game, "Sram, Senior Edificer", "Aether Revolt", "AER");
  }
}

module.exports = SramSeniorEdificer;
