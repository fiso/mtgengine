"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CaughtintheBrights extends UnimplementedCard {
  constructor (game) {
    super(game, "Caught in the Brights", "Aether Revolt", "AER");
  }
}

module.exports = CaughtintheBrights;
