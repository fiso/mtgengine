"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ReservoirWalker extends UnimplementedCard {
  constructor (game) {
    super(game, "Reservoir Walker", "Aether Revolt", "AER");
  }
}

module.exports = ReservoirWalker;
