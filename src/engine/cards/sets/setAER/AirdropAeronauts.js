"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AirdropAeronauts extends UnimplementedCard {
  constructor (game) {
    super(game, "Airdrop Aeronauts", "Aether Revolt", "AER");
  }
}

module.exports = AirdropAeronauts;
