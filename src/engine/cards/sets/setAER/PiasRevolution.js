"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PiasRevolution extends UnimplementedCard {
  constructor (game) {
    super(game, "Pia's Revolution", "Aether Revolt", "AER");
  }
}

module.exports = PiasRevolution;
