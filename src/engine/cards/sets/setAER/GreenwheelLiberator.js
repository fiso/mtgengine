"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GreenwheelLiberator extends UnimplementedCard {
  constructor (game) {
    super(game, "Greenwheel Liberator", "Aether Revolt", "AER");
  }
}

module.exports = GreenwheelLiberator;
