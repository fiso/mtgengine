"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UntetheredExpress extends UnimplementedCard {
  constructor (game) {
    super(game, "Untethered Express", "Aether Revolt", "AER");
  }
}

module.exports = UntetheredExpress;
