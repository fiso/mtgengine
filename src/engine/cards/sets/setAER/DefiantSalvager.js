"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DefiantSalvager extends UnimplementedCard {
  constructor (game) {
    super(game, "Defiant Salvager", "Aether Revolt", "AER");
  }
}

module.exports = DefiantSalvager;
