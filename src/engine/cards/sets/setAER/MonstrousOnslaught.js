"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MonstrousOnslaught extends UnimplementedCard {
  constructor (game) {
    super(game, "Monstrous Onslaught", "Aether Revolt", "AER");
  }
}

module.exports = MonstrousOnslaught;
