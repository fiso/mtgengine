"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PacificationArray extends UnimplementedCard {
  constructor (game) {
    super(game, "Pacification Array", "Aether Revolt", "AER");
  }
}

module.exports = PacificationArray;
