"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SalvageScuttler extends UnimplementedCard {
  constructor (game) {
    super(game, "Salvage Scuttler", "Aether Revolt", "AER");
  }
}

module.exports = SalvageScuttler;
