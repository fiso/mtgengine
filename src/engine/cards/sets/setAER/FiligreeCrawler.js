"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FiligreeCrawler extends UnimplementedCard {
  constructor (game) {
    super(game, "Filigree Crawler", "Aether Revolt", "AER");
  }
}

module.exports = FiligreeCrawler;
