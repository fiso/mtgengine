"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class IllusionistsStratagem extends UnimplementedCard {
  constructor (game) {
    super(game, "Illusionist's Stratagem", "Aether Revolt", "AER");
  }
}

module.exports = IllusionistsStratagem;
