"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CadaverousBloom extends UnimplementedCard {
  constructor (game) {
    super(game, "Cadaverous Bloom", "Mirage", "MIR");
  }
}

module.exports = CadaverousBloom;
