"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class KukemssaSerpent extends UnimplementedCard {
  constructor (game) {
    super(game, "Kukemssa Serpent", "Mirage", "MIR");
  }
}

module.exports = KukemssaSerpent;
