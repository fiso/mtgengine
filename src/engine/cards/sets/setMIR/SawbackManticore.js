"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SawbackManticore extends UnimplementedCard {
  constructor (game) {
    super(game, "Sawback Manticore", "Mirage", "MIR");
  }
}

module.exports = SawbackManticore;
