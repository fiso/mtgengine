"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DirtwaterWraith extends UnimplementedCard {
  constructor (game) {
    super(game, "Dirtwater Wraith", "Mirage", "MIR");
  }
}

module.exports = DirtwaterWraith;
