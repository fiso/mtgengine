"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UrborgPanther extends UnimplementedCard {
  constructor (game) {
    super(game, "Urborg Panther", "Mirage", "MIR");
  }
}

module.exports = UrborgPanther;
