"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TranquilDomain extends UnimplementedCard {
  constructor (game) {
    super(game, "Tranquil Domain", "Mirage", "MIR");
  }
}

module.exports = TranquilDomain;
