"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TorrentofLava extends UnimplementedCard {
  constructor (game) {
    super(game, "Torrent of Lava", "Mirage", "MIR");
  }
}

module.exports = TorrentofLava;
