"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LuminousWake extends UnimplementedCard {
  constructor (game) {
    super(game, "Luminous Wake", "Rise of the Eldrazi", "ROE");
  }
}

module.exports = LuminousWake;
