"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ShatteredDreams extends UnimplementedCard {
  constructor (game) {
    super(game, "Shattered Dreams", "Fifth Dawn", "5DN");
  }
}

module.exports = ShatteredDreams;
