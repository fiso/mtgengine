"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class InfusedArrows extends UnimplementedCard {
  constructor (game) {
    super(game, "Infused Arrows", "Fifth Dawn", "5DN");
  }
}

module.exports = InfusedArrows;
