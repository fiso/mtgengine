"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BerserkMurlodont extends UnimplementedCard {
  constructor (game) {
    super(game, "Berserk Murlodont", "Legions", "LGN");
  }
}

module.exports = BerserkMurlodont;
