"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AerieOuphes extends UnimplementedCard {
  constructor (game) {
    super(game, "Aerie Ouphes", "Eventide", "EVE");
  }
}

module.exports = AerieOuphes;
