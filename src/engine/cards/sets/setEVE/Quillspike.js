"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Quillspike extends UnimplementedCard {
  constructor (game) {
    super(game, "Quillspike", "Eventide", "EVE");
  }
}

module.exports = Quillspike;
