"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HeartlashCinder extends UnimplementedCard {
  constructor (game) {
    super(game, "Heartlash Cinder", "Eventide", "EVE");
  }
}

module.exports = HeartlashCinder;
