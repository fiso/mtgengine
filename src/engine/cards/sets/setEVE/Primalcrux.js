"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Primalcrux extends UnimplementedCard {
  constructor (game) {
    super(game, "Primalcrux", "Eventide", "EVE");
  }
}

module.exports = Primalcrux;
