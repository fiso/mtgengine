"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeityofScars extends UnimplementedCard {
  constructor (game) {
    super(game, "Deity of Scars", "Eventide", "EVE");
  }
}

module.exports = DeityofScars;
