"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class StigmaLasher extends UnimplementedCard {
  constructor (game) {
    super(game, "Stigma Lasher", "Eventide", "EVE");
  }
}

module.exports = StigmaLasher;
