"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RazorfinAbolisher extends UnimplementedCard {
  constructor (game) {
    super(game, "Razorfin Abolisher", "Eventide", "EVE");
  }
}

module.exports = RazorfinAbolisher;
