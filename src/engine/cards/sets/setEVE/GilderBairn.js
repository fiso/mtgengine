"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GilderBairn extends UnimplementedCard {
  constructor (game) {
    super(game, "Gilder Bairn", "Eventide", "EVE");
  }
}

module.exports = GilderBairn;
