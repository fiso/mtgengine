"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HagHedgeMage extends UnimplementedCard {
  constructor (game) {
    super(game, "Hag Hedge-Mage", "Eventide", "EVE");
  }
}

module.exports = HagHedgeMage;
