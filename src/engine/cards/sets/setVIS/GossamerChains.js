"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GossamerChains extends UnimplementedCard {
  constructor (game) {
    super(game, "Gossamer Chains", "Visions", "VIS");
  }
}

module.exports = GossamerChains;
