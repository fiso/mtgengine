"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SuleimansLegacy extends UnimplementedCard {
  constructor (game) {
    super(game, "Suleiman's Legacy", "Visions", "VIS");
  }
}

module.exports = SuleimansLegacy;
