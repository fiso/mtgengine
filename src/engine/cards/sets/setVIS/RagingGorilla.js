"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RagingGorilla extends UnimplementedCard {
  constructor (game) {
    super(game, "Raging Gorilla", "Visions", "VIS");
  }
}

module.exports = RagingGorilla;
