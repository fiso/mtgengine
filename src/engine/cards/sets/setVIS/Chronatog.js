"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Chronatog extends UnimplementedCard {
  constructor (game) {
    super(game, "Chronatog", "Visions", "VIS");
  }
}

module.exports = Chronatog;
