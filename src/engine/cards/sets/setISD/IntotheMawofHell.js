"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class IntotheMawofHell extends UnimplementedCard {
  constructor (game) {
    super(game, "Into the Maw of Hell", "Innistrad", "ISD");
  }
}

module.exports = IntotheMawofHell;
