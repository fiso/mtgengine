"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CloisteredYouthUnholyFiend extends UnimplementedCard {
  constructor (game) {
    super(game, "Cloistered Youth // Unholy Fiend", "Innistrad", "ISD");
  }
}

module.exports = CloisteredYouthUnholyFiend;
