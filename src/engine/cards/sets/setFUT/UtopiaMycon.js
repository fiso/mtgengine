"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UtopiaMycon extends UnimplementedCard {
  constructor (game) {
    super(game, "Utopia Mycon", "Future Sight", "FUT");
  }
}

module.exports = UtopiaMycon;
