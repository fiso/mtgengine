"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpellwildOuphe extends UnimplementedCard {
  constructor (game) {
    super(game, "Spellwild Ouphe", "Future Sight", "FUT");
  }
}

module.exports = SpellwildOuphe;
