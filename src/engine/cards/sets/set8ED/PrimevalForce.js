"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PrimevalForce extends UnimplementedCard {
  constructor (game) {
    super(game, "Primeval Force", "Eighth Edition", "8ED");
  }
}

module.exports = PrimevalForce;
