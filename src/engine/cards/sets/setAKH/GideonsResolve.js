"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GideonsResolve extends UnimplementedCard {
  constructor (game) {
    super(game, "Gideon's Resolve", "Amonkhet", "AKH");
  }
}

module.exports = GideonsResolve;
