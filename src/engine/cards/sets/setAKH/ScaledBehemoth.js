"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ScaledBehemoth extends UnimplementedCard {
  constructor (game) {
    super(game, "Scaled Behemoth", "Amonkhet", "AKH");
  }
}

module.exports = ScaledBehemoth;
