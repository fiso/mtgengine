"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DjerusResolve extends UnimplementedCard {
  constructor (game) {
    super(game, "Djeru's Resolve", "Amonkhet", "AKH");
  }
}

module.exports = DjerusResolve;
