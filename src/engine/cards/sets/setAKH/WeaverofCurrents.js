"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WeaverofCurrents extends UnimplementedCard {
  constructor (game) {
    super(game, "Weaver of Currents", "Amonkhet", "AKH");
  }
}

module.exports = WeaverofCurrents;
