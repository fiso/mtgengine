"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AhnCropCrasher extends UnimplementedCard {
  constructor (game) {
    super(game, "Ahn-Crop Crasher", "Amonkhet", "AKH");
  }
}

module.exports = AhnCropCrasher;
