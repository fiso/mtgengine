"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RagsRiches extends UnimplementedCard {
  constructor (game) {
    super(game, "Rags // Riches", "Amonkhet", "AKH");
  }
}

module.exports = RagsRiches;
