"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpringMind extends UnimplementedCard {
  constructor (game) {
    super(game, "Spring // Mind", "Amonkhet", "AKH");
  }
}

module.exports = SpringMind;
