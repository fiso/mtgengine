"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class OpenintoWonder extends UnimplementedCard {
  constructor (game) {
    super(game, "Open into Wonder", "Amonkhet", "AKH");
  }
}

module.exports = OpenintoWonder;
