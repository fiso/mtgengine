"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AngrathsAmbusher extends UnimplementedCard {
  constructor (game) {
    super(game, "Angrath's Ambusher", "Rivals of Ixalan", "RIX");
  }
}

module.exports = AngrathsAmbusher;
