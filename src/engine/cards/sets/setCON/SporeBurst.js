"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SporeBurst extends UnimplementedCard {
  constructor (game) {
    super(game, "Spore Burst", "Conflux", "CON");
  }
}

module.exports = SporeBurst;
