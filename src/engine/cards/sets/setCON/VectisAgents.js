"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VectisAgents extends UnimplementedCard {
  constructor (game) {
    super(game, "Vectis Agents", "Conflux", "CON");
  }
}

module.exports = VectisAgents;
