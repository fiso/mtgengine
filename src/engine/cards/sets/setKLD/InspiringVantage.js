"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class InspiringVantage extends UnimplementedCard {
  constructor (game) {
    super(game, "Inspiring Vantage", "Kaladesh", "KLD");
  }
}

module.exports = InspiringVantage;
