"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ConsulsShieldguard extends UnimplementedCard {
  constructor (game) {
    super(game, "Consul's Shieldguard", "Kaladesh", "KLD");
  }
}

module.exports = ConsulsShieldguard;
