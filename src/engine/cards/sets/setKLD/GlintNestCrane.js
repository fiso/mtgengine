"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GlintNestCrane extends UnimplementedCard {
  constructor (game) {
    super(game, "Glint-Nest Crane", "Kaladesh", "KLD");
  }
}

module.exports = GlintNestCrane;
