"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VerdurousGearhulk extends UnimplementedCard {
  constructor (game) {
    super(game, "Verdurous Gearhulk", "Kaladesh", "KLD");
  }
}

module.exports = VerdurousGearhulk;
