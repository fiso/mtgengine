"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class KeytotheCity extends UnimplementedCard {
  constructor (game) {
    super(game, "Key to the City", "Kaladesh", "KLD");
  }
}

module.exports = KeytotheCity;
