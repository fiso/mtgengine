"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CombustibleGearhulk extends UnimplementedCard {
  constructor (game) {
    super(game, "Combustible Gearhulk", "Kaladesh", "KLD");
  }
}

module.exports = CombustibleGearhulk;
