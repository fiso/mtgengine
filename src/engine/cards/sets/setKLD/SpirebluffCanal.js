"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpirebluffCanal extends UnimplementedCard {
  constructor (game) {
    super(game, "Spirebluff Canal", "Kaladesh", "KLD");
  }
}

module.exports = SpirebluffCanal;
