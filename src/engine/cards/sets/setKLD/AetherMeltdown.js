"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AetherMeltdown extends UnimplementedCard {
  constructor (game) {
    super(game, "Aether Meltdown", "Kaladesh", "KLD");
  }
}

module.exports = AetherMeltdown;
