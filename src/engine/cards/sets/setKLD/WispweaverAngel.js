"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WispweaverAngel extends UnimplementedCard {
  constructor (game) {
    super(game, "Wispweaver Angel", "Kaladesh", "KLD");
  }
}

module.exports = WispweaverAngel;
