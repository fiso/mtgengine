"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Whirlermaker extends UnimplementedCard {
  constructor (game) {
    super(game, "Whirlermaker", "Kaladesh", "KLD");
  }
}

module.exports = Whirlermaker;
