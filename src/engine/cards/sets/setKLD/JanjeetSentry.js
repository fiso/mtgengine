"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class JanjeetSentry extends UnimplementedCard {
  constructor (game) {
    super(game, "Janjeet Sentry", "Kaladesh", "KLD");
  }
}

module.exports = JanjeetSentry;
