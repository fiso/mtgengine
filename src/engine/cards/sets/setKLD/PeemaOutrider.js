"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PeemaOutrider extends UnimplementedCard {
  constructor (game) {
    super(game, "Peema Outrider", "Kaladesh", "KLD");
  }
}

module.exports = PeemaOutrider;
