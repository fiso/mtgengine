"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AppetitefortheUnnatural extends UnimplementedCard {
  constructor (game) {
    super(game, "Appetite for the Unnatural", "Kaladesh", "KLD");
  }
}

module.exports = AppetitefortheUnnatural;
