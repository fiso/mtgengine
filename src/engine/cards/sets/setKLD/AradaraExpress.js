"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AradaraExpress extends UnimplementedCard {
  constructor (game) {
    super(game, "Aradara Express", "Kaladesh", "KLD");
  }
}

module.exports = AradaraExpress;
