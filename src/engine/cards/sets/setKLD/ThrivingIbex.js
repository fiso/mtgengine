"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ThrivingIbex extends UnimplementedCard {
  constructor (game) {
    super(game, "Thriving Ibex", "Kaladesh", "KLD");
  }
}

module.exports = ThrivingIbex;
