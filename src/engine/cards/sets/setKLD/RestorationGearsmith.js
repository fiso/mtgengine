"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RestorationGearsmith extends UnimplementedCard {
  constructor (game) {
    super(game, "Restoration Gearsmith", "Kaladesh", "KLD");
  }
}

module.exports = RestorationGearsmith;
