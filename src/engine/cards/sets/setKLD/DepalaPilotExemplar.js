"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DepalaPilotExemplar extends UnimplementedCard {
  constructor (game) {
    super(game, "Depala, Pilot Exemplar", "Kaladesh", "KLD");
  }
}

module.exports = DepalaPilotExemplar;
