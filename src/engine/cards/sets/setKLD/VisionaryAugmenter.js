"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VisionaryAugmenter extends UnimplementedCard {
  constructor (game) {
    super(game, "Visionary Augmenter", "Kaladesh", "KLD");
  }
}

module.exports = VisionaryAugmenter;
