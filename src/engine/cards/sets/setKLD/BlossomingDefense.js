"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BlossomingDefense extends UnimplementedCard {
  constructor (game) {
    super(game, "Blossoming Defense", "Kaladesh", "KLD");
  }
}

module.exports = BlossomingDefense;
