"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EmbraalBruiser extends UnimplementedCard {
  constructor (game) {
    super(game, "Embraal Bruiser", "Kaladesh", "KLD");
  }
}

module.exports = EmbraalBruiser;
