"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MisguidedRage extends UnimplementedCard {
  constructor (game) {
    super(game, "Misguided Rage", "Scourge", "SCG");
  }
}

module.exports = MisguidedRage;
