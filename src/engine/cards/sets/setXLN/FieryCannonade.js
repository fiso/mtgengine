"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FieryCannonade extends UnimplementedCard {
  constructor (game) {
    super(game, "Fiery Cannonade", "Ixalan", "XLN");
  }
}

module.exports = FieryCannonade;
