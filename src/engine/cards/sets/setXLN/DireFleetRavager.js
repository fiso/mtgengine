"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DireFleetRavager extends UnimplementedCard {
  constructor (game) {
    super(game, "Dire Fleet Ravager", "Ixalan", "XLN");
  }
}

module.exports = DireFleetRavager;
