"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CastawaysDespair extends UnimplementedCard {
  constructor (game) {
    super(game, "Castaway's Despair", "Ixalan", "XLN");
  }
}

module.exports = CastawaysDespair;
