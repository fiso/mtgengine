"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AngrathsMarauders extends UnimplementedCard {
  constructor (game) {
    super(game, "Angrath's Marauders", "Ixalan", "XLN");
  }
}

module.exports = AngrathsMarauders;
