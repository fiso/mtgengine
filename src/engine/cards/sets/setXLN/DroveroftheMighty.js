"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DroveroftheMighty extends UnimplementedCard {
  constructor (game) {
    super(game, "Drover of the Mighty", "Ixalan", "XLN");
  }
}

module.exports = DroveroftheMighty;
