"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class NavigatorsRuin extends UnimplementedCard {
  constructor (game) {
    super(game, "Navigator's Ruin", "Ixalan", "XLN");
  }
}

module.exports = NavigatorsRuin;
