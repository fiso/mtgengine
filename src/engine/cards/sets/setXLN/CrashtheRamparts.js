"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CrashtheRamparts extends UnimplementedCard {
  constructor (game) {
    super(game, "Crash the Ramparts", "Ixalan", "XLN");
  }
}

module.exports = CrashtheRamparts;
