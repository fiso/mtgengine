"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HeadstrongBrute extends UnimplementedCard {
  constructor (game) {
    super(game, "Headstrong Brute", "Ixalan", "XLN");
  }
}

module.exports = HeadstrongBrute;
