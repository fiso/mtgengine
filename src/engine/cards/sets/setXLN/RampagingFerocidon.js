"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RampagingFerocidon extends UnimplementedCard {
  constructor (game) {
    super(game, "Rampaging Ferocidon", "Ixalan", "XLN");
  }
}

module.exports = RampagingFerocidon;
