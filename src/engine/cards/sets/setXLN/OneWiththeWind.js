"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class OneWiththeWind extends UnimplementedCard {
  constructor (game) {
    super(game, "One With the Wind", "Ixalan", "XLN");
  }
}

module.exports = OneWiththeWind;
