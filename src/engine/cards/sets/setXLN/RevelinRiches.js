"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RevelinRiches extends UnimplementedCard {
  constructor (game) {
    super(game, "Revel in Riches", "Ixalan", "XLN");
  }
}

module.exports = RevelinRiches;
