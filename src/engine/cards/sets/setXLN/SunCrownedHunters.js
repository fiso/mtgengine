"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SunCrownedHunters extends UnimplementedCard {
  constructor (game) {
    super(game, "Sun-Crowned Hunters", "Ixalan", "XLN");
  }
}

module.exports = SunCrownedHunters;
