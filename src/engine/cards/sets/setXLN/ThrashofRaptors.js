"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ThrashofRaptors extends UnimplementedCard {
  constructor (game) {
    super(game, "Thrash of Raptors", "Ixalan", "XLN");
  }
}

module.exports = ThrashofRaptors;
