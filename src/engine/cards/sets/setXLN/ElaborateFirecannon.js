"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ElaborateFirecannon extends UnimplementedCard {
  constructor (game) {
    super(game, "Elaborate Firecannon", "Ixalan", "XLN");
  }
}

module.exports = ElaborateFirecannon;
