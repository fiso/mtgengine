"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WakeroftheWilds extends UnimplementedCard {
  constructor (game) {
    super(game, "Waker of the Wilds", "Ixalan", "XLN");
  }
}

module.exports = WakeroftheWilds;
