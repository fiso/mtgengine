"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DistortingWake extends UnimplementedCard {
  constructor (game) {
    super(game, "Distorting Wake", "Commander 2014", "C14");
  }
}

module.exports = DistortingWake;
