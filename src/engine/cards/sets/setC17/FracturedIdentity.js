"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FracturedIdentity extends UnimplementedCard {
  constructor (game) {
    super(game, "Fractured Identity", "Commander 2017", "C17");
  }
}

module.exports = FracturedIdentity;
