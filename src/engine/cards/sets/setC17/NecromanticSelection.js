"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class NecromanticSelection extends UnimplementedCard {
  constructor (game) {
    super(game, "Necromantic Selection", "Commander 2017", "C17");
  }
}

module.exports = NecromanticSelection;
