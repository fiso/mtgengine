"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ReaveSoul extends UnimplementedCard {
  constructor (game) {
    super(game, "Reave Soul", "Iconic Masters", "IMA");
  }
}

module.exports = ReaveSoul;
