"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SplatterThug extends UnimplementedCard {
  constructor (game) {
    super(game, "Splatter Thug", "Iconic Masters", "IMA");
  }
}

module.exports = SplatterThug;
