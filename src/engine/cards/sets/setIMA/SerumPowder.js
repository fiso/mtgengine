"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SerumPowder extends UnimplementedCard {
  constructor (game) {
    super(game, "Serum Powder", "Iconic Masters", "IMA");
  }
}

module.exports = SerumPowder;
