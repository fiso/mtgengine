"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Ulcerate extends UnimplementedCard {
  constructor (game) {
    super(game, "Ulcerate", "Iconic Masters", "IMA");
  }
}

module.exports = Ulcerate;
