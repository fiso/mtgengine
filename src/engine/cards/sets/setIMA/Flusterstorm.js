"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Flusterstorm extends UnimplementedCard {
  constructor (game) {
    super(game, "Flusterstorm", "Iconic Masters", "IMA");
  }
}

module.exports = Flusterstorm;
