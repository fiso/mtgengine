"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PathofBravery extends UnimplementedCard {
  constructor (game) {
    super(game, "Path of Bravery", "Iconic Masters", "IMA");
  }
}

module.exports = PathofBravery;
