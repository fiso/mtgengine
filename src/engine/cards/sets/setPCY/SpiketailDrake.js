"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpiketailDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Spiketail Drake", "Prophecy", "PCY");
  }
}

module.exports = SpiketailDrake;
