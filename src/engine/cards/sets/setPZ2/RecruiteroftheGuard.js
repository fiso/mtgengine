"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RecruiteroftheGuard extends UnimplementedCard {
  constructor (game) {
    super(game, "Recruiter of the Guard", "You Make the Cube", "PZ2");
  }
}

module.exports = RecruiteroftheGuard;
