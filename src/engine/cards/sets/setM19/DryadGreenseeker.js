"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DryadGreenseeker extends UnimplementedCard {
  constructor (game) {
    super(game, "Dryad Greenseeker", "Core Set 2019", "M19");
  }
}

module.exports = DryadGreenseeker;
