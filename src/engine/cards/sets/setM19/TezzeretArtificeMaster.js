"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TezzeretArtificeMaster extends UnimplementedCard {
  constructor (game) {
    super(game, "Tezzeret, Artifice Master", "Core Set 2019", "M19");
  }
}

module.exports = TezzeretArtificeMaster;
