"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RabidBite extends UnimplementedCard {
  constructor (game) {
    super(game, "Rabid Bite", "Core Set 2019", "M19");
  }
}

module.exports = RabidBite;
