"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Omenspeaker extends UnimplementedCard {
  constructor (game) {
    super(game, "Omenspeaker", "Core Set 2019", "M19");
  }
}

module.exports = Omenspeaker;
