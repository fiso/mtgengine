"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AjanisInfluence extends UnimplementedCard {
  constructor (game) {
    super(game, "Ajani's Influence", "Core Set 2019", "M19");
  }
}

module.exports = AjanisInfluence;
