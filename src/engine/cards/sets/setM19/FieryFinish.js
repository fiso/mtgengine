"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FieryFinish extends UnimplementedCard {
  constructor (game) {
    super(game, "Fiery Finish", "Core Set 2019", "M19");
  }
}

module.exports = FieryFinish;
