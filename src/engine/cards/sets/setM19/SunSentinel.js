"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SunSentinel extends UnimplementedCard {
  constructor (game) {
    super(game, "Sun Sentinel", "Core Set 2019", "M19");
  }
}

module.exports = SunSentinel;
