"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ActofTreason extends UnimplementedCard {
  constructor (game) {
    super(game, "Act of Treason", "Core Set 2019", "M19");
  }
}

module.exports = ActofTreason;
