"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Guttersnipe extends UnimplementedCard {
  constructor (game) {
    super(game, "Guttersnipe", "Core Set 2019", "M19");
  }
}

module.exports = Guttersnipe;
