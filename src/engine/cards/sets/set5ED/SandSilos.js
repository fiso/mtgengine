'use strict';
const Constants = require('../../../Constants');
const UnimplementedCard = require('../../UnimplementedCard');

class SandSilos extends UnimplementedCard {
  constructor (game) {
    super(game, 'Sand Silos', 'Fifth Edition', '5ED');
  }
}

module.exports = SandSilos;
