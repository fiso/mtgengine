"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Worldpurge extends UnimplementedCard {
  constructor (game) {
    super(game, "Worldpurge", "Shadowmoor", "SHM");
  }
}

module.exports = Worldpurge;
