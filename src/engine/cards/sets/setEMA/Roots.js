"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Roots extends UnimplementedCard {
  constructor (game) {
    super(game, "Roots", "Eternal Masters", "EMA");
  }
}

module.exports = Roots;
