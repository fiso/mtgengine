"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Mournwillow extends UnimplementedCard {
  constructor (game) {
    super(game, "Mournwillow", "Eldritch Moon", "EMN");
  }
}

module.exports = Mournwillow;
