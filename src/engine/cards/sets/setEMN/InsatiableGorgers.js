"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class InsatiableGorgers extends UnimplementedCard {
  constructor (game) {
    super(game, "Insatiable Gorgers", "Eldritch Moon", "EMN");
  }
}

module.exports = InsatiableGorgers;
