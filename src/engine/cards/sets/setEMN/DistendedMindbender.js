"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DistendedMindbender extends UnimplementedCard {
  constructor (game) {
    super(game, "Distended Mindbender", "Eldritch Moon", "EMN");
  }
}

module.exports = DistendedMindbender;
