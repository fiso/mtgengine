"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PermeatingMass extends UnimplementedCard {
  constructor (game) {
    super(game, "Permeating Mass", "Eldritch Moon", "EMN");
  }
}

module.exports = PermeatingMass;
