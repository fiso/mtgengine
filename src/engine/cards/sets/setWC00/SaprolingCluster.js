"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SaprolingCluster extends UnimplementedCard {
  constructor (game) {
    super(game, "Saproling Cluster", "World Championship Decks 2000", "WC00");
  }
}

module.exports = SaprolingCluster;
