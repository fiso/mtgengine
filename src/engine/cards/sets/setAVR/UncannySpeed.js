"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UncannySpeed extends UnimplementedCard {
  constructor (game) {
    super(game, "Uncanny Speed", "Avacyn Restored", "AVR");
  }
}

module.exports = UncannySpeed;
