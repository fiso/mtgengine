"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RDsSecretLair extends UnimplementedCard {
  constructor (game) {
    super(game, "R&D's Secret Lair", "Unhinged", "UNH");
  }
}

module.exports = RDsSecretLair;
