"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AssWhuppin extends UnimplementedCard {
  constructor (game) {
    super(game, "Ass Whuppin'", "Unhinged", "UNH");
  }
}

module.exports = AssWhuppin;
