"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MonssGoblinWaiters extends UnimplementedCard {
  constructor (game) {
    super(game, "Mons's Goblin Waiters", "Unhinged", "UNH");
  }
}

module.exports = MonssGoblinWaiters;
