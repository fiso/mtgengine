"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Gleemax extends UnimplementedCard {
  constructor (game) {
    super(game, "Gleemax", "Unhinged", "UNH");
  }
}

module.exports = Gleemax;
