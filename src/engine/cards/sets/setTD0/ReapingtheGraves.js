"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ReapingtheGraves extends UnimplementedCard {
  constructor (game) {
    super(game, "Reaping the Graves", "Magic Online Theme Decks", "TD0");
  }
}

module.exports = ReapingtheGraves;
