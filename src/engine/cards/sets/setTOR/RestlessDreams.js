"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RestlessDreams extends UnimplementedCard {
  constructor (game) {
    super(game, "Restless Dreams", "Torment", "TOR");
  }
}

module.exports = RestlessDreams;
