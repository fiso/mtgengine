"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GrusildaMonsterMasher extends UnimplementedCard {
  constructor (game) {
    super(game, "Grusilda, Monster Masher", "Unstable", "UST");
  }
}

module.exports = GrusildaMonsterMasher;
