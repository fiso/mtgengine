"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UrzasScienceFairProject extends UnimplementedCard {
  constructor (game) {
    super(game, "Urza's Science Fair Project", "Unglued", "UGL");
  }
}

module.exports = UrzasScienceFairProject;
