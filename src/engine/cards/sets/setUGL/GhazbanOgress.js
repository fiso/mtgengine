"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GhazbanOgress extends UnimplementedCard {
  constructor (game) {
    super(game, "Ghazbán Ogress", "Unglued", "UGL");
  }
}

module.exports = GhazbanOgress;
