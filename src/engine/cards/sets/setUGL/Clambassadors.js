"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Clambassadors extends UnimplementedCard {
  constructor (game) {
    super(game, "Clambassadors", "Unglued", "UGL");
  }
}

module.exports = Clambassadors;
