"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HinterlandLoggerTimberShredder extends UnimplementedCard {
  constructor (game) {
    super(game, "Hinterland Logger // Timber Shredder", "Shadows over Innistrad", "SOI");
  }
}

module.exports = HinterlandLoggerTimberShredder;
