"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Gloomwidow extends UnimplementedCard {
  constructor (game) {
    super(game, "Gloomwidow", "Shadows over Innistrad", "SOI");
  }
}

module.exports = Gloomwidow;
