"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HarvestHandScroungedScythe extends UnimplementedCard {
  constructor (game) {
    super(game, "Harvest Hand // Scrounged Scythe", "Shadows over Innistrad", "SOI");
  }
}

module.exports = HarvestHandScroungedScythe;
