"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DiregrafColossus extends UnimplementedCard {
  constructor (game) {
    super(game, "Diregraf Colossus", "Shadows over Innistrad", "SOI");
  }
}

module.exports = DiregrafColossus;
