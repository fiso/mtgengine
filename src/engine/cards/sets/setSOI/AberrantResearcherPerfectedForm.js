"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AberrantResearcherPerfectedForm extends UnimplementedCard {
  constructor (game) {
    super(game, "Aberrant Researcher // Perfected Form", "Shadows over Innistrad", "SOI");
  }
}

module.exports = AberrantResearcherPerfectedForm;
