"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Electropotence extends UnimplementedCard {
  constructor (game) {
    super(game, "Electropotence", "Zendikar", "ZEN");
  }
}

module.exports = Electropotence;
