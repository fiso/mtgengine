"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UginsConstruct extends UnimplementedCard {
  constructor (game) {
    super(game, "Ugin's Construct", "Fate Reforged", "FRF");
  }
}

module.exports = UginsConstruct;
