"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Vaultbreaker extends UnimplementedCard {
  constructor (game) {
    super(game, "Vaultbreaker", "Fate Reforged", "FRF");
  }
}

module.exports = Vaultbreaker;
