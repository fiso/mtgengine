"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GurmagAngler extends UnimplementedCard {
  constructor (game) {
    super(game, "Gurmag Angler", "Fate Reforged", "FRF");
  }
}

module.exports = GurmagAngler;
