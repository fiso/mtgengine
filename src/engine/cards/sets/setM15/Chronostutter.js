"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Chronostutter extends UnimplementedCard {
  constructor (game) {
    super(game, "Chronostutter", "Magic 2015", "M15");
  }
}

module.exports = Chronostutter;
