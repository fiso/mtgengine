"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HarabazDruid extends UnimplementedCard {
  constructor (game) {
    super(game, "Harabaz Druid", "Worldwake", "WWK");
  }
}

module.exports = HarabazDruid;
