"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HorizonDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Horizon Drake", "Worldwake", "WWK");
  }
}

module.exports = HorizonDrake;
