"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TideforceElemental extends UnimplementedCard {
  constructor (game) {
    super(game, "Tideforce Elemental", "Worldwake", "WWK");
  }
}

module.exports = TideforceElemental;
