"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ParasiticStrix extends UnimplementedCard {
  constructor (game) {
    super(game, "Parasitic Strix", "Conflux", "CON");
  }
}

module.exports = ParasiticStrix;
