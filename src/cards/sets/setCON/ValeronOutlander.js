"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ValeronOutlander extends UnimplementedCard {
  constructor (game) {
    super(game, "Valeron Outlander", "Conflux", "CON");
  }
}

module.exports = ValeronOutlander;
