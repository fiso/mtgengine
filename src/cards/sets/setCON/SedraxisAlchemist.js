"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SedraxisAlchemist extends UnimplementedCard {
  constructor (game) {
    super(game, "Sedraxis Alchemist", "Conflux", "CON");
  }
}

module.exports = SedraxisAlchemist;
