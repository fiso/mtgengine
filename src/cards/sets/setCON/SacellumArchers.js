"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SacellumArchers extends UnimplementedCard {
  constructor(game) {
    super(game, "Sacellum Archers", "Conflux", "CON");
  }
}

module.exports = SacellumArchers;
