"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AcidWebSpider extends UnimplementedCard {
  constructor (game) {
    super(game, "Acid Web Spider", "Scars of Mirrodin", "SOM");
  }
}

module.exports = AcidWebSpider;
