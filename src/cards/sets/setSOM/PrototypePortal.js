"use strict";
const Constants = require ("../../../Constants");
const PrototypePortalBase = require("../setC18/PrototypePortal");

class PrototypePortal extends PrototypePortalBase {
  constructor (game) {
    super(game, "Prototype Portal", "Scars of Mirrodin", "SOM");
  }
}

module.exports = PrototypePortal;
