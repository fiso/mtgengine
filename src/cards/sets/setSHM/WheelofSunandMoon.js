"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WheelofSunandMoon extends UnimplementedCard {
  constructor (game) {
    super(game, "Wheel of Sun and Moon", "Shadowmoor", "SHM");
  }
}

module.exports = WheelofSunandMoon;
