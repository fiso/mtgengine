"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class IslebackSpawn extends UnimplementedCard {
  constructor (game) {
    super(game, "Isleback Spawn", "Shadowmoor", "SHM");
  }
}

module.exports = IslebackSpawn;
