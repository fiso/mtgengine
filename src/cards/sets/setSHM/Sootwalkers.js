"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Sootwalkers extends UnimplementedCard {
  constructor(game) {
    super(game, "Sootwalkers", "Shadowmoor", "SHM");
  }
}

module.exports = Sootwalkers;
