"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GlamerSpinners extends UnimplementedCard {
  constructor (game) {
    super(game, "Glamer Spinners", "Shadowmoor", "SHM");
  }
}

module.exports = GlamerSpinners;
