"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AphoticWisps extends UnimplementedCard {
  constructor(game) {
    super(game, "Aphotic Wisps", "Shadowmoor", "SHM");
  }
}

module.exports = AphoticWisps;
