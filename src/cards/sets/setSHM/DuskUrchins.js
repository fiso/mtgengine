"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DuskUrchins extends UnimplementedCard {
  constructor (game) {
    super(game, "Dusk Urchins", "Shadowmoor", "SHM");
  }
}

module.exports = DuskUrchins;
