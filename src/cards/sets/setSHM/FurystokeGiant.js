"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FurystokeGiant extends UnimplementedCard {
  constructor (game) {
    super(game, "Furystoke Giant", "Shadowmoor", "SHM");
  }
}

module.exports = FurystokeGiant;
