"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VastwoodAnimist extends UnimplementedCard {
  constructor (game) {
    super(game, "Vastwood Animist", "Worldwake", "WWK");
  }
}

module.exports = VastwoodAnimist;
