"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RagingRavine extends UnimplementedCard {
  constructor (game) {
    super(game, "Raging Ravine", "Worldwake", "WWK");
  }
}

module.exports = RagingRavine;
