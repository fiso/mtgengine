"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Tanglebloom extends UnimplementedCard {
  constructor (game) {
    super(game, "Tanglebloom", "Ninth Edition", "9ED");
  }
}

module.exports = Tanglebloom;
