"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SatyrGrovedancer extends UnimplementedCard {
  constructor (game) {
    super(game, "Satyr Grovedancer", "Journey into Nyx", "JOU");
  }
}

module.exports = SatyrGrovedancer;
