"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GnarledScarhide extends UnimplementedCard {
  constructor(game) {
    super(game, "Gnarled Scarhide", "Journey into Nyx", "JOU");
  }
}

module.exports = GnarledScarhide;
