"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ZephyrScribe extends UnimplementedCard {
  constructor (game) {
    super(game, "Zephyr Scribe", "Dragons of Tarkir", "DTK");
  }
}

module.exports = ZephyrScribe;
