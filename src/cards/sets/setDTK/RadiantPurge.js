"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RadiantPurge extends UnimplementedCard {
  constructor(game) {
    super(game, "Radiant Purge", "Dragons of Tarkir", "DTK");
  }
}

module.exports = RadiantPurge;
