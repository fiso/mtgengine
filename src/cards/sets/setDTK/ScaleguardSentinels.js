"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ScaleguardSentinels extends UnimplementedCard {
  constructor (game) {
    super(game, "Scaleguard Sentinels", "Dragons of Tarkir", "DTK");
  }
}

module.exports = ScaleguardSentinels;
