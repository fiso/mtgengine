"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DragonlordSilumgar extends UnimplementedCard {
  constructor (game) {
    super(game, "Dragonlord Silumgar", "Dragons of Tarkir", "DTK");
  }
}

module.exports = DragonlordSilumgar;
