"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PrismaticGeoscope extends UnimplementedCard {
  constructor (game) {
    super(game, "Prismatic Geoscope", "Judge Gift Cards 2017", "J17");
  }
}

module.exports = PrismaticGeoscope;
