"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ExultantSkymarcher extends UnimplementedCard {
  constructor (game) {
    super(game, "Exultant Skymarcher", "Rivals of Ixalan", "RIX");
  }
}

module.exports = ExultantSkymarcher;
