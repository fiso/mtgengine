"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ReiverDemon extends UnimplementedCard {
  constructor (game) {
    super(game, "Reiver Demon", "Commander Anthology", "CMA");
  }
}

module.exports = ReiverDemon;
