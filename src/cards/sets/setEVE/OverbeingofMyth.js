"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class OverbeingofMyth extends UnimplementedCard {
  constructor (game) {
    super(game, "Overbeing of Myth", "Eventide", "EVE");
  }
}

module.exports = OverbeingofMyth;
