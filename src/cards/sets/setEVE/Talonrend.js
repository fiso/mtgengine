"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Talonrend extends UnimplementedCard {
  constructor (game) {
    super(game, "Talonrend", "Eventide", "EVE");
  }
}

module.exports = Talonrend;
