"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HobgoblinDragoon extends UnimplementedCard {
  constructor(game) {
    super(game, "Hobgoblin Dragoon", "Eventide", "EVE");
  }
}

module.exports = HobgoblinDragoon;
