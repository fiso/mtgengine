"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RendclawTrow extends UnimplementedCard {
  constructor (game) {
    super(game, "Rendclaw Trow", "Eventide", "EVE");
  }
}

module.exports = RendclawTrow;
