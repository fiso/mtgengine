"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WindgraceAcolyte extends UnimplementedCard {
  constructor (game) {
    super(game, "Windgrace Acolyte", "Dominaria", "DOM");
  }
}

module.exports = WindgraceAcolyte;
