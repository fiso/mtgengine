"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TeferiTimebender extends UnimplementedCard {
  constructor (game) {
    super(game, "Teferi, Timebender", "Dominaria", "DOM");
  }
}

module.exports = TeferiTimebender;
