"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SongofFreyalise extends UnimplementedCard {
  constructor (game) {
    super(game, "Song of Freyalise", "Dominaria", "DOM");
  }
}

module.exports = SongofFreyalise;
