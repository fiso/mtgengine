"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class KarplusanHound extends UnimplementedCard {
  constructor (game) {
    super(game, "Karplusan Hound", "Dominaria", "DOM");
  }
}

module.exports = KarplusanHound;
