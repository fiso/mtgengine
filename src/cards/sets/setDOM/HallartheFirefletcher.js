"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HallartheFirefletcher extends UnimplementedCard {
  constructor (game) {
    super(game, "Hallar, the Firefletcher", "Dominaria", "DOM");
  }
}

module.exports = HallartheFirefletcher;
