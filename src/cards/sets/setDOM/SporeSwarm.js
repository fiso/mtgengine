"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SporeSwarm extends UnimplementedCard {
  constructor (game) {
    super(game, "Spore Swarm", "Dominaria", "DOM");
  }
}

module.exports = SporeSwarm;
