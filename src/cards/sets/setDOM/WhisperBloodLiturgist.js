"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WhisperBloodLiturgist extends UnimplementedCard {
  constructor (game) {
    super(game, "Whisper, Blood Liturgist", "Dominaria", "DOM");
  }
}

module.exports = WhisperBloodLiturgist;
