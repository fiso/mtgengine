"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class KarnsTemporalSundering extends UnimplementedCard {
  constructor (game) {
    super(game, "Karn's Temporal Sundering", "Dominaria", "DOM");
  }
}

module.exports = KarnsTemporalSundering;
