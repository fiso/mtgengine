"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DesolateLighthouse extends UnimplementedCard {
  constructor (game) {
    super(game, "Desolate Lighthouse", "Avacyn Restored", "AVR");
  }
}

module.exports = DesolateLighthouse;
