"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpirePatrol extends UnimplementedCard {
  constructor (game) {
    super(game, "Spire Patrol", "Aether Revolt", "AER");
  }
}

module.exports = SpirePatrol;
