"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UnbridledGrowth extends UnimplementedCard {
  constructor (game) {
    super(game, "Unbridled Growth", "Aether Revolt", "AER");
  }
}

module.exports = UnbridledGrowth;
