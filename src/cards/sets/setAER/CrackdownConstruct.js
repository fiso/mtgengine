"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CrackdownConstruct extends UnimplementedCard {
  constructor (game) {
    super(game, "Crackdown Construct", "Aether Revolt", "AER");
  }
}

module.exports = CrackdownConstruct;
