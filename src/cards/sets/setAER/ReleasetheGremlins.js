"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ReleasetheGremlins extends UnimplementedCard {
  constructor (game) {
    super(game, "Release the Gremlins", "Aether Revolt", "AER");
  }
}

module.exports = ReleasetheGremlins;
