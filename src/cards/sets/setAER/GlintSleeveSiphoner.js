"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GlintSleeveSiphoner extends UnimplementedCard {
  constructor (game) {
    super(game, "Glint-Sleeve Siphoner", "Aether Revolt", "AER");
  }
}

module.exports = GlintSleeveSiphoner;
