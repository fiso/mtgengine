"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RogueRefiner extends UnimplementedCard {
  constructor (game) {
    super(game, "Rogue Refiner", "Aether Revolt", "AER");
  }
}

module.exports = RogueRefiner;
