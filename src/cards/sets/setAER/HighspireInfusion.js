"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HighspireInfusion extends UnimplementedCard {
  constructor (game) {
    super(game, "Highspire Infusion", "Aether Revolt", "AER");
  }
}

module.exports = HighspireInfusion;
