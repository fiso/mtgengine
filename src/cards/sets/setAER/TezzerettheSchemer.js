"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TezzerettheSchemer extends UnimplementedCard {
  constructor (game) {
    super(game, "Tezzeret the Schemer", "Aether Revolt", "AER");
  }
}

module.exports = TezzerettheSchemer;
