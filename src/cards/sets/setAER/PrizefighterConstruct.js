"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PrizefighterConstruct extends UnimplementedCard {
  constructor (game) {
    super(game, "Prizefighter Construct", "Aether Revolt", "AER");
  }
}

module.exports = PrizefighterConstruct;
