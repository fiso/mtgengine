"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HinterlandDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Hinterland Drake", "Aether Revolt", "AER");
  }
}

module.exports = HinterlandDrake;
