"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VerdantAutomaton extends UnimplementedCard {
  constructor (game) {
    super(game, "Verdant Automaton", "Aether Revolt", "AER");
  }
}

module.exports = VerdantAutomaton;
