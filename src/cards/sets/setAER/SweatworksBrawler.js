"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SweatworksBrawler extends UnimplementedCard {
  constructor (game) {
    super(game, "Sweatworks Brawler", "Aether Revolt", "AER");
  }
}

module.exports = SweatworksBrawler;
