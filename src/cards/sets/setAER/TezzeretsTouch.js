"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TezzeretsTouch extends UnimplementedCard {
  constructor (game) {
    super(game, "Tezzeret's Touch", "Aether Revolt", "AER");
  }
}

module.exports = TezzeretsTouch;
