"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ResourcefulReturn extends UnimplementedCard {
  constructor (game) {
    super(game, "Resourceful Return", "Aether Revolt", "AER");
  }
}

module.exports = ResourcefulReturn;
