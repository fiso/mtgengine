"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ConsulateCrackdown extends UnimplementedCard {
  constructor (game) {
    super(game, "Consulate Crackdown", "Aether Revolt", "AER");
  }
}

module.exports = ConsulateCrackdown;
