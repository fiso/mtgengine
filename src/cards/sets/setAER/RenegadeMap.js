"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RenegadeMap extends UnimplementedCard {
  constructor (game) {
    super(game, "Renegade Map", "Aether Revolt", "AER");
  }
}

module.exports = RenegadeMap;
