"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DawnfeatherEagle extends UnimplementedCard {
  constructor (game) {
    super(game, "Dawnfeather Eagle", "Aether Revolt", "AER");
  }
}

module.exports = DawnfeatherEagle;
