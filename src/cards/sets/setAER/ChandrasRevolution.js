"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ChandrasRevolution extends UnimplementedCard {
  constructor (game) {
    super(game, "Chandra's Revolution", "Aether Revolt", "AER");
  }
}

module.exports = ChandrasRevolution;
