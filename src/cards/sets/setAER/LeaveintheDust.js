"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LeaveintheDust extends UnimplementedCard {
  constructor (game) {
    super(game, "Leave in the Dust", "Aether Revolt", "AER");
  }
}

module.exports = LeaveintheDust;
