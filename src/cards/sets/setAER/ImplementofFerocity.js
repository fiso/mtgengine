"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ImplementofFerocity extends UnimplementedCard {
  constructor (game) {
    super(game, "Implement of Ferocity", "Aether Revolt", "AER");
  }
}

module.exports = ImplementofFerocity;
