"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class IrontreadCrusher extends UnimplementedCard {
  constructor (game) {
    super(game, "Irontread Crusher", "Aether Revolt", "AER");
  }
}

module.exports = IrontreadCrusher;
