"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SongofBlood extends UnimplementedCard {
  constructor (game) {
    super(game, "Song of Blood", "Visions", "VIS");
  }
}

module.exports = SongofBlood;
