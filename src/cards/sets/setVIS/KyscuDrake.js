"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class KyscuDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Kyscu Drake", "Visions", "VIS");
  }
}

module.exports = KyscuDrake;
