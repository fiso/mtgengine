"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DreamTides extends UnimplementedCard {
  constructor(game) {
    super(game, "Dream Tides", "Visions", "VIS");
  }
}

module.exports = DreamTides;
