"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Desolation extends UnimplementedCard {
  constructor(game) {
    super(game, "Desolation", "Visions", "VIS");
  }
}

module.exports = Desolation;
