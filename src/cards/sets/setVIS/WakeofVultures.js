"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WakeofVultures extends UnimplementedCard {
  constructor (game) {
    super(game, "Wake of Vultures", "Visions", "VIS");
  }
}

module.exports = WakeofVultures;
