"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UntaidaketheCloudKeeper extends UnimplementedCard {
  constructor (game) {
    super(game, "Untaidake, the Cloud Keeper", "Champions of Kamigawa", "CHK");
  }
}

module.exports = UntaidaketheCloudKeeper;
