"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Crawlspace extends UnimplementedCard {
  constructor(game) {
    super(game, "Crawlspace", "Commander 2013 Edition", "C13");
  }
}

module.exports = Crawlspace;
