"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TidalForce extends UnimplementedCard {
  constructor(game) {
    super(game, "Tidal Force", "Commander 2013 Edition", "C13");
  }
}

module.exports = TidalForce;
