"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Staggershock extends UnimplementedCard {
  constructor (game) {
    super(game, "Staggershock", "Iconic Masters", "IMA");
  }
}

module.exports = Staggershock;
