"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RiftBolt extends UnimplementedCard {
  constructor (game) {
    super(game, "Rift Bolt", "Iconic Masters", "IMA");
  }
}

module.exports = RiftBolt;
