"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Necropotence extends UnimplementedCard {
  constructor (game) {
    super(game, "Necropotence", "Iconic Masters", "IMA");
  }
}

module.exports = Necropotence;
