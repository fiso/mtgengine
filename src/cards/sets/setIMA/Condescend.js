"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Condescend extends UnimplementedCard {
  constructor (game) {
    super(game, "Condescend", "Iconic Masters", "IMA");
  }
}

module.exports = Condescend;
