"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class IllusoryAngel extends UnimplementedCard {
  constructor (game) {
    super(game, "Illusory Angel", "Iconic Masters", "IMA");
  }
}

module.exports = IllusoryAngel;
