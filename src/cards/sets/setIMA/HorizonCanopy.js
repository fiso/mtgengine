"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HorizonCanopy extends UnimplementedCard {
  constructor (game) {
    super(game, "Horizon Canopy", "Iconic Masters", "IMA");
  }
}

module.exports = HorizonCanopy;
