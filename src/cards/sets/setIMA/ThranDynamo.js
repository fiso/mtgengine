"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ThranDynamo extends UnimplementedCard {
  constructor (game) {
    super(game, "Thran Dynamo", "Iconic Masters", "IMA");
  }
}

module.exports = ThranDynamo;
