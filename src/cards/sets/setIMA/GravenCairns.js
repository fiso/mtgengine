"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GravenCairns extends UnimplementedCard {
  constructor (game) {
    super(game, "Graven Cairns", "Iconic Masters", "IMA");
  }
}

module.exports = GravenCairns;
