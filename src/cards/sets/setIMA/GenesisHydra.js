"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GenesisHydra extends UnimplementedCard {
  constructor (game) {
    super(game, "Genesis Hydra", "Iconic Masters", "IMA");
  }
}

module.exports = GenesisHydra;
