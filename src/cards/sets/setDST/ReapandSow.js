"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ReapandSow extends UnimplementedCard {
  constructor (game) {
    super(game, "Reap and Sow", "Darksteel", "DST");
  }
}

module.exports = ReapandSow;
