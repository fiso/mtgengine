"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LichLordofUnx extends UnimplementedCard {
  constructor(game) {
    super(game, "Lich Lord of Unx", "Alara Reborn", "ARB");
  }
}

module.exports = LichLordofUnx;
