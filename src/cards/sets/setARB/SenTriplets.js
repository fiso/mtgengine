"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SenTriplets extends UnimplementedCard {
  constructor (game) {
    super(game, "Sen Triplets", "Alara Reborn", "ARB");
  }
}

module.exports = SenTriplets;
