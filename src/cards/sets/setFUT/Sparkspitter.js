"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Sparkspitter extends UnimplementedCard {
  constructor(game) {
    super(game, "Sparkspitter", "Future Sight", "FUT");
  }
}

module.exports = Sparkspitter;
