"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SkizzikSurger extends UnimplementedCard {
  constructor (game) {
    super(game, "Skizzik Surger", "Future Sight", "FUT");
  }
}

module.exports = SkizzikSurger;
