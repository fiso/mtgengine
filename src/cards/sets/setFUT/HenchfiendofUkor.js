"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HenchfiendofUkor extends UnimplementedCard {
  constructor (game) {
    super(game, "Henchfiend of Ukor", "Future Sight", "FUT");
  }
}

module.exports = HenchfiendofUkor;
