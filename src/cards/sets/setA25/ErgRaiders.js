"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ErgRaiders extends UnimplementedCard {
  constructor (game) {
    super(game, "Erg Raiders", "Masters 25", "A25");
  }
}

module.exports = ErgRaiders;
