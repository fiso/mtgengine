"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Cetavolver extends UnimplementedCard {
  constructor(game) {
    super(game, "Cetavolver", "Apocalypse", "APC");
  }
}

module.exports = Cetavolver;
