"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SphereofReason extends UnimplementedCard {
  constructor (game) {
    super(game, "Sphere of Reason", "Odyssey", "ODY");
  }
}

module.exports = SphereofReason;
