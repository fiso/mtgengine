"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Tombfire extends UnimplementedCard {
  constructor (game) {
    super(game, "Tombfire", "Odyssey", "ODY");
  }
}

module.exports = Tombfire;
