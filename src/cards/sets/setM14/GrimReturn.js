"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GrimReturn extends UnimplementedCard {
  constructor (game) {
    super(game, "Grim Return", "Magic 2014 Core Set", "M14");
  }
}

module.exports = GrimReturn;
