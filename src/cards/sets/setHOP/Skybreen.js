"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Skybreen extends UnimplementedCard {
  constructor (game) {
    super(game, "Skybreen", "Planechase", "HOP");
  }
}

module.exports = Skybreen;
