"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GustcloakRunner extends UnimplementedCard {
  constructor (game) {
    super(game, "Gustcloak Runner", "Onslaught", "ONS");
  }
}

module.exports = GustcloakRunner;
