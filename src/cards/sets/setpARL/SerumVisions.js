"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SerumVisions extends UnimplementedCard {
  constructor(game) {
    super(game, "Serum Visions", "Arena League", "pARL");
  }
}

module.exports = SerumVisions;
