"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CatalystElemental extends UnimplementedCard {
  constructor (game) {
    super(game, "Catalyst Elemental", "Core Set 2019", "M19");
  }
}

module.exports = CatalystElemental;
