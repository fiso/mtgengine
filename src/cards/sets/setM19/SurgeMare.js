"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SurgeMare extends UnimplementedCard {
  constructor (game) {
    super(game, "Surge Mare", "Core Set 2019", "M19");
  }
}

module.exports = SurgeMare;
