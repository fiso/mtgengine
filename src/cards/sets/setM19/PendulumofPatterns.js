"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PendulumofPatterns extends UnimplementedCard {
  constructor (game) {
    super(game, "Pendulum of Patterns", "Core Set 2019", "M19");
  }
}

module.exports = PendulumofPatterns;
