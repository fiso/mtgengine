"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GiantSpider extends UnimplementedCard {
  constructor (game) {
    super(game, "Giant Spider", "Core Set 2019", "M19");
  }
}

module.exports = GiantSpider;
