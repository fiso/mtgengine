"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AjanisLastStand extends UnimplementedCard {
  constructor (game) {
    super(game, "Ajani's Last Stand", "Core Set 2019", "M19");
  }
}

module.exports = AjanisLastStand;
