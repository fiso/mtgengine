"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LightningMare extends UnimplementedCard {
  constructor (game) {
    super(game, "Lightning Mare", "Core Set 2019", "M19");
  }
}

module.exports = LightningMare;
