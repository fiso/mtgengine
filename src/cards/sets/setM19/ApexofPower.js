"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ApexofPower extends UnimplementedCard {
  constructor (game) {
    super(game, "Apex of Power", "Core Set 2019", "M19");
  }
}

module.exports = ApexofPower;
