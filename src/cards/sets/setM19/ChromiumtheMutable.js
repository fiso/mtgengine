"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ChromiumtheMutable extends UnimplementedCard {
  constructor (game) {
    super(game, "Chromium, the Mutable", "Core Set 2019", "M19");
  }
}

module.exports = ChromiumtheMutable;
