"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PlagueMare extends UnimplementedCard {
  constructor (game) {
    super(game, "Plague Mare", "Core Set 2019", "M19");
  }
}

module.exports = PlagueMare;
