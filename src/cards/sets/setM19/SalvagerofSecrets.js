"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SalvagerofSecrets extends UnimplementedCard {
  constructor (game) {
    super(game, "Salvager of Secrets", "Core Set 2019", "M19");
  }
}

module.exports = SalvagerofSecrets;
