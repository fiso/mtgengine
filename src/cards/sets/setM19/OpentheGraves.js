"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class OpentheGraves extends UnimplementedCard {
  constructor (game) {
    super(game, "Open the Graves", "Core Set 2019", "M19");
  }
}

module.exports = OpentheGraves;
