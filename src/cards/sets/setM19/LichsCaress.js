"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LichsCaress extends UnimplementedCard {
  constructor (game) {
    super(game, "Lich's Caress", "Core Set 2019", "M19");
  }
}

module.exports = LichsCaress;
