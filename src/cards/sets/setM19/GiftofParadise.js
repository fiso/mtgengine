"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GiftofParadise extends UnimplementedCard {
  constructor (game) {
    super(game, "Gift of Paradise", "Core Set 2019", "M19");
  }
}

module.exports = GiftofParadise;
