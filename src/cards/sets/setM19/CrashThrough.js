"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CrashThrough extends UnimplementedCard {
  constructor (game) {
    super(game, "Crash Through", "Core Set 2019", "M19");
  }
}

module.exports = CrashThrough;
