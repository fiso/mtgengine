"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GoblinInstigator extends UnimplementedCard {
  constructor (game) {
    super(game, "Goblin Instigator", "Core Set 2019", "M19");
  }
}

module.exports = GoblinInstigator;
