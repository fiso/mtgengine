"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Scapeshift extends UnimplementedCard {
  constructor (game) {
    super(game, "Scapeshift", "Core Set 2019", "M19");
  }
}

module.exports = Scapeshift;
