"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Petrahydrox extends UnimplementedCard {
  constructor (game) {
    super(game, "Petrahydrox", "Guildpact", "GPT");
  }
}

module.exports = Petrahydrox;
