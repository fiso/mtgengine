"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Aetherplasm extends UnimplementedCard {
  constructor (game) {
    super(game, "Ætherplasm", "Guildpact", "GPT");
  }
}

module.exports = Aetherplasm;
