"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PredatoryFocus extends UnimplementedCard {
  constructor (game) {
    super(game, "Predatory Focus", "Guildpact", "GPT");
  }
}

module.exports = PredatoryFocus;
