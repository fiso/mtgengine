"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AbsolverThrull extends UnimplementedCard {
  constructor (game) {
    super(game, "Absolver Thrull", "Guildpact", "GPT");
  }
}

module.exports = AbsolverThrull;
