"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TalismanofImpulse extends UnimplementedCard {
  constructor (game) {
    super(game, "Talisman of Impulse", "Mirrodin", "MRD");
  }
}

module.exports = TalismanofImpulse;
