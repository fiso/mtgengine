"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CullingScales extends UnimplementedCard {
  constructor (game) {
    super(game, "Culling Scales", "Mirrodin", "MRD");
  }
}

module.exports = CullingScales;
