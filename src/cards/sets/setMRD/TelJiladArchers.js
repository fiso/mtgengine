"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TelJiladArchers extends UnimplementedCard {
  constructor (game) {
    super(game, "Tel-Jilad Archers", "Mirrodin", "MRD");
  }
}

module.exports = TelJiladArchers;
