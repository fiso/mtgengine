"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SkyhunterCub extends UnimplementedCard {
  constructor (game) {
    super(game, "Skyhunter Cub", "Mirrodin", "MRD");
  }
}

module.exports = SkyhunterCub;
