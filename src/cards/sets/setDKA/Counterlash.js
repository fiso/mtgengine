"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Counterlash extends UnimplementedCard {
  constructor (game) {
    super(game, "Counterlash", "Dark Ascension", "DKA");
  }
}

module.exports = Counterlash;
