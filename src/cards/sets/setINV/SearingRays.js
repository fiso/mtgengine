"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SearingRays extends UnimplementedCard {
  constructor (game) {
    super(game, "Searing Rays", "Invasion", "INV");
  }
}

module.exports = SearingRays;
