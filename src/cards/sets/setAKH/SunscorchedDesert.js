"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SunscorchedDesert extends UnimplementedCard {
  constructor (game) {
    super(game, "Sunscorched Desert", "Amonkhet", "AKH");
  }
}

module.exports = SunscorchedDesert;
