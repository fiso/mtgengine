"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UnwaveringInitiate extends UnimplementedCard {
  constructor (game) {
    super(game, "Unwavering Initiate", "Amonkhet", "AKH");
  }
}

module.exports = UnwaveringInitiate;
