"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ScatteredGroves extends UnimplementedCard {
  constructor (game) {
    super(game, "Scattered Groves", "Amonkhet", "AKH");
  }
}

module.exports = ScatteredGroves;
