"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GatetotheAfterlife extends UnimplementedCard {
  constructor (game) {
    super(game, "Gate to the Afterlife", "Amonkhet", "AKH");
  }
}

module.exports = GatetotheAfterlife;
