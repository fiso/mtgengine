"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Colossapede extends UnimplementedCard {
  constructor (game) {
    super(game, "Colossapede", "Amonkhet", "AKH");
  }
}

module.exports = Colossapede;
