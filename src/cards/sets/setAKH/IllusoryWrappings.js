"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class IllusoryWrappings extends UnimplementedCard {
  constructor (game) {
    super(game, "Illusory Wrappings", "Amonkhet", "AKH");
  }
}

module.exports = IllusoryWrappings;
