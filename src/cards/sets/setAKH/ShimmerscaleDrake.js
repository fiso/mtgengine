"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ShimmerscaleDrake extends UnimplementedCard {
  constructor (game) {
    super(game, "Shimmerscale Drake", "Amonkhet", "AKH");
  }
}

module.exports = ShimmerscaleDrake;
