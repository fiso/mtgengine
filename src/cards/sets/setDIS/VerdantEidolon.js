"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VerdantEidolon extends UnimplementedCard {
  constructor (game) {
    super(game, "Verdant Eidolon", "Dissension", "DIS");
  }
}

module.exports = VerdantEidolon;
