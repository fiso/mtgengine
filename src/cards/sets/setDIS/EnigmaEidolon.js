"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EnigmaEidolon extends UnimplementedCard {
  constructor (game) {
    super(game, "Enigma Eidolon", "Dissension", "DIS");
  }
}

module.exports = EnigmaEidolon;
