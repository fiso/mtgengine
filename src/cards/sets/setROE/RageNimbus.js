"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RageNimbus extends UnimplementedCard {
  constructor(game) {
    super(game, "Rage Nimbus", "Rise of the Eldrazi", "ROE");
  }
}

module.exports = RageNimbus;
