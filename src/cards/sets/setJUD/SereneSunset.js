"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SereneSunset extends UnimplementedCard {
  constructor (game) {
    super(game, "Serene Sunset", "Judgment", "JUD");
  }
}

module.exports = SereneSunset;
