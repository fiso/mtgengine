"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AnuridSwarmsnapper extends UnimplementedCard {
  constructor(game) {
    super(game, "Anurid Swarmsnapper", "Judgment", "JUD");
  }
}

module.exports = AnuridSwarmsnapper;
