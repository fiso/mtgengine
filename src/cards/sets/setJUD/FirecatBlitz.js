"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FirecatBlitz extends UnimplementedCard {
  constructor(game) {
    super(game, "Firecat Blitz", "Judgment", "JUD");
  }
}

module.exports = FirecatBlitz;
