"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SoulgorgerOrgg extends UnimplementedCard {
  constructor(game) {
    super(game, "Soulgorger Orgg", "Judgment", "JUD");
  }
}

module.exports = SoulgorgerOrgg;
