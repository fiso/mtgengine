"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CrushofWurms extends UnimplementedCard {
  constructor (game) {
    super(game, "Crush of Wurms", "Judgment", "JUD");
  }
}

module.exports = CrushofWurms;
