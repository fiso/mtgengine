"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LaquatussDisdain extends UnimplementedCard {
  constructor (game) {
    super(game, "Laquatus's Disdain", "Judgment", "JUD");
  }
}

module.exports = LaquatussDisdain;
