"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UrborgJustice extends UnimplementedCard {
  constructor(game) {
    super(game, "Urborg Justice", "Weatherlight", "WTH");
  }
}

module.exports = UrborgJustice;
