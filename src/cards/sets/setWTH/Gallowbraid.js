"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Gallowbraid extends UnimplementedCard {
  constructor (game) {
    super(game, "Gallowbraid", "Weatherlight", "WTH");
  }
}

module.exports = Gallowbraid;
