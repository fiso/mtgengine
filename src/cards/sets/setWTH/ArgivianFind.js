"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ArgivianFind extends UnimplementedCard {
  constructor(game) {
    super(game, "Argivian Find", "Weatherlight", "WTH");
  }
}

module.exports = ArgivianFind;
