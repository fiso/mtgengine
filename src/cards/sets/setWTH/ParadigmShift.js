"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ParadigmShift extends UnimplementedCard {
  constructor (game) {
    super(game, "Paradigm Shift", "Weatherlight", "WTH");
  }
}

module.exports = ParadigmShift;
