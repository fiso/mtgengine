"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HivisoftheScale extends UnimplementedCard {
  constructor(game) {
    super(game, "Hivis of the Scale", "Mirage", "MIR");
  }
}

module.exports = HivisoftheScale;
