"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PhyrexianPurge extends UnimplementedCard {
  constructor(game) {
    super(game, "Phyrexian Purge", "Mirage", "MIR");
  }
}

module.exports = PhyrexianPurge;
