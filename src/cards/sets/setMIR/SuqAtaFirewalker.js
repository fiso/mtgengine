"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SuqAtaFirewalker extends UnimplementedCard {
  constructor (game) {
    super(game, "Suq'Ata Firewalker", "Mirage", "MIR");
  }
}

module.exports = SuqAtaFirewalker;
