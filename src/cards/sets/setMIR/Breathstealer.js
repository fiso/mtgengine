"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Breathstealer extends UnimplementedCard {
  constructor (game) {
    super(game, "Breathstealer", "Mirage", "MIR");
  }
}

module.exports = Breathstealer;
