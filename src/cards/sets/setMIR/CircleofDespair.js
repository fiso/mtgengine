"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CircleofDespair extends UnimplementedCard {
  constructor (game) {
    super(game, "Circle of Despair", "Mirage", "MIR");
  }
}

module.exports = CircleofDespair;
