"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PrismaticLace extends UnimplementedCard {
  constructor (game) {
    super(game, "Prismatic Lace", "Mirage", "MIR");
  }
}

module.exports = PrismaticLace;
