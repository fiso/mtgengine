"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FetidHorror extends UnimplementedCard {
  constructor (game) {
    super(game, "Fetid Horror", "Mirage", "MIR");
  }
}

module.exports = FetidHorror;
