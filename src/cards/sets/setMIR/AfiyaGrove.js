"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AfiyaGrove extends UnimplementedCard {
  constructor (game) {
    super(game, "Afiya Grove", "Mirage", "MIR");
  }
}

module.exports = AfiyaGrove;
