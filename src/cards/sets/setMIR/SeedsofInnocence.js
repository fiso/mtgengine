"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SeedsofInnocence extends UnimplementedCard {
  constructor (game) {
    super(game, "Seeds of Innocence", "Mirage", "MIR");
  }
}

module.exports = SeedsofInnocence;
