"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VerdantForce extends UnimplementedCard {
  constructor (game) {
    super(game, "Verdant Force", "Commander 2015", "C15");
  }
}

module.exports = VerdantForce;
