"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class NovijenHeartofProgress extends UnimplementedCard {
  constructor (game) {
    super(game, "Novijen, Heart of Progress", "Commander 2015", "C15");
  }
}

module.exports = NovijenHeartofProgress;
