"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PyrostaticPillar extends UnimplementedCard {
  constructor (game) {
    super(game, "Pyrostatic Pillar", "Magic Online Theme Decks", "TD0");
  }
}

module.exports = PyrostaticPillar;
