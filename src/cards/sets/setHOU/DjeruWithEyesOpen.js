"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DjeruWithEyesOpen extends UnimplementedCard {
  constructor (game) {
    super(game, "Djeru, With Eyes Open", "Hour of Devastation", "HOU");
  }
}

module.exports = DjeruWithEyesOpen;
