"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FlowstoneSurge extends UnimplementedCard {
  constructor (game) {
    super(game, "Flowstone Surge", "Nemesis", "NEM");
  }
}

module.exports = FlowstoneSurge;
