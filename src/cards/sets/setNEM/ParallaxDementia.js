"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ParallaxDementia extends UnimplementedCard {
  constructor (game) {
    super(game, "Parallax Dementia", "Nemesis", "NEM");
  }
}

module.exports = ParallaxDementia;
