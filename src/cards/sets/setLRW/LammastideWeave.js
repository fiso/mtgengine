"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LammastideWeave extends UnimplementedCard {
  constructor (game) {
    super(game, "Lammastide Weave", "Lorwyn", "LRW");
  }
}

module.exports = LammastideWeave;
