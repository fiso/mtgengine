"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MerrowHarbinger extends UnimplementedCard {
  constructor (game) {
    super(game, "Merrow Harbinger", "Lorwyn", "LRW");
  }
}

module.exports = MerrowHarbinger;
