"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Rootgrapple extends UnimplementedCard {
  constructor(game) {
    super(game, "Rootgrapple", "Lorwyn", "LRW");
  }
}

module.exports = Rootgrapple;
