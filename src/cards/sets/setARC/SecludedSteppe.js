"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SecludedSteppe extends UnimplementedCard {
  constructor (game) {
    super(game, "Secluded Steppe", "Archenemy", "ARC");
  }
}

module.exports = SecludedSteppe;
