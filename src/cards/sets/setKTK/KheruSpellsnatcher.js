"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class KheruSpellsnatcher extends UnimplementedCard {
  constructor (game) {
    super(game, "Kheru Spellsnatcher", "Khans of Tarkir", "KTK");
  }
}

module.exports = KheruSpellsnatcher;
