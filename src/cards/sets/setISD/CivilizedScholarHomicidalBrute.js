"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CivilizedScholarHomicidalBrute extends UnimplementedCard {
  constructor (game) {
    super(game, "Civilized Scholar // Homicidal Brute", "Innistrad", "ISD");
  }
}

module.exports = CivilizedScholarHomicidalBrute;
