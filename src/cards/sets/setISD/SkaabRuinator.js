"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SkaabRuinator extends UnimplementedCard {
  constructor (game) {
    super(game, "Skaab Ruinator", "Innistrad", "ISD");
  }
}

module.exports = SkaabRuinator;
