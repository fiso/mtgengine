"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FeralRidgewolf extends UnimplementedCard {
  constructor (game) {
    super(game, "Feral Ridgewolf", "Innistrad", "ISD");
  }
}

module.exports = FeralRidgewolf;
