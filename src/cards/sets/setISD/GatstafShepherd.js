"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GatstafShepherd extends UnimplementedCard {
  constructor(game) {
    super(game, "Gatstaf Shepherd", "Innistrad", "ISD");
  }
}

module.exports = GatstafShepherd;
