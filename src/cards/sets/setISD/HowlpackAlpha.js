"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HowlpackAlpha extends UnimplementedCard {
  constructor (game) {
    super(game, "Howlpack Alpha", "Innistrad", "ISD");
  }
}

module.exports = HowlpackAlpha;
