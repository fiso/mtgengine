"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BloodcrazedNeonate extends UnimplementedCard {
  constructor (game) {
    super(game, "Bloodcrazed Neonate", "Innistrad", "ISD");
  }
}

module.exports = BloodcrazedNeonate;
