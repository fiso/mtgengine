"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DaybreakRanger extends UnimplementedCard {
  constructor (game) {
    super(game, "Daybreak Ranger", "Innistrad", "ISD");
  }
}

module.exports = DaybreakRanger;
