"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PurifytheGrave extends UnimplementedCard {
  constructor(game) {
    super(game, "Purify the Grave", "Innistrad", "ISD");
  }
}

module.exports = PurifytheGrave;
