"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Paraselene extends UnimplementedCard {
  constructor (game) {
    super(game, "Paraselene", "Innistrad", "ISD");
  }
}

module.exports = Paraselene;
