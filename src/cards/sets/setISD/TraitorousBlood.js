"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TraitorousBlood extends UnimplementedCard {
  constructor (game) {
    super(game, "Traitorous Blood", "Innistrad", "ISD");
  }
}

module.exports = TraitorousBlood;
