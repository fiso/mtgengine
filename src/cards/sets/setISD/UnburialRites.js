"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class UnburialRites extends UnimplementedCard {
  constructor (game) {
    super(game, "Unburial Rites", "Innistrad", "ISD");
  }
}

module.exports = UnburialRites;
