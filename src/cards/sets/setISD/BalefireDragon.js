"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BalefireDragon extends UnimplementedCard {
  constructor (game) {
    super(game, "Balefire Dragon", "Innistrad", "ISD");
  }
}

module.exports = BalefireDragon;
