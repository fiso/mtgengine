"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GrizzledOutcasts extends UnimplementedCard {
  constructor(game) {
    super(game, "Grizzled Outcasts", "Innistrad", "ISD");
  }
}

module.exports = GrizzledOutcasts;
