"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FullMoonsRise extends UnimplementedCard {
  constructor (game) {
    super(game, "Full Moon's Rise", "Innistrad", "ISD");
  }
}

module.exports = FullMoonsRise;
