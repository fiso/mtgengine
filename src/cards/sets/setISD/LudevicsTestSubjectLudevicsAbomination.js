"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LudevicsTestSubjectLudevicsAbomination extends UnimplementedCard {
  constructor (game) {
    super(game, "Ludevic's Test Subject // Ludevic's Abomination", "Innistrad", "ISD");
  }
}

module.exports = LudevicsTestSubjectLudevicsAbomination;
