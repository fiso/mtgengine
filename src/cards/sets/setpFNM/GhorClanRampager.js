"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GhorClanRampager extends UnimplementedCard {
  constructor (game) {
    super(game, "Ghor-Clan Rampager", "Friday Night Magic", "pFNM");
  }
}

module.exports = GhorClanRampager;
