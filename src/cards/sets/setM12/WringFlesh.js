"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WringFlesh extends UnimplementedCard {
  constructor(game) {
    super(game, "Wring Flesh", "Magic 2012", "M12");
  }
}

module.exports = WringFlesh;
