"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GroundSeal extends UnimplementedCard {
  constructor (game) {
    super(game, "Ground Seal", "Commander 2018", "C18");
  }
}

module.exports = GroundSeal;
