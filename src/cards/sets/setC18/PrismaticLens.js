"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PrismaticLens extends UnimplementedCard {
  constructor (game) {
    super(game, "Prismatic Lens", "Commander 2018", "C18");
  }
}

module.exports = PrismaticLens;
