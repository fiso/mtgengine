"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SaheelisDirective extends UnimplementedCard {
  constructor (game) {
    super(game, "Saheeli's Directive", "Commander 2018", "C18");
  }
}

module.exports = SaheelisDirective;
