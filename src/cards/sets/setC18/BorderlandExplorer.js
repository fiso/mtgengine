"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BorderlandExplorer extends UnimplementedCard {
  constructor (game) {
    super(game, "Borderland Explorer", "Commander 2018", "C18");
  }
}

module.exports = BorderlandExplorer;
