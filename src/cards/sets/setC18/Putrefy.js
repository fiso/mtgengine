"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Putrefy extends UnimplementedCard {
  constructor (game) {
    super(game, "Putrefy", "Commander 2018", "C18");
  }
}

module.exports = Putrefy;
