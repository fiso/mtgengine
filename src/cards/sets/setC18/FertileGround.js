"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FertileGround extends UnimplementedCard {
  constructor (game) {
    super(game, "Fertile Ground", "Commander 2018", "C18");
  }
}

module.exports = FertileGround;
