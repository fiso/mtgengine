"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GruulTurf extends UnimplementedCard {
  constructor (game) {
    super(game, "Gruul Turf", "Commander 2018", "C18");
  }
}

module.exports = GruulTurf;
