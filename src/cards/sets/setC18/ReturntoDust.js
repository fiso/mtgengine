"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ReturntoDust extends UnimplementedCard {
  constructor (game) {
    super(game, "Return to Dust", "Commander 2018", "C18");
  }
}

module.exports = ReturntoDust;
