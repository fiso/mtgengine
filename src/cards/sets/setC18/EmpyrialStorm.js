"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EmpyrialStorm extends UnimplementedCard {
  constructor (game) {
    super(game, "Empyrial Storm", "Commander 2018", "C18");
  }
}

module.exports = EmpyrialStorm;
