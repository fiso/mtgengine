"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MagmaticForce extends UnimplementedCard {
  constructor (game) {
    super(game, "Magmatic Force", "Battlebond", "BBD");
  }
}

module.exports = MagmaticForce;
