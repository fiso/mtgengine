"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PathwayArrows extends UnimplementedCard {
  constructor (game) {
    super(game, "Pathway Arrows", "Battle for Zendikar", "BFZ");
  }
}

module.exports = PathwayArrows;
