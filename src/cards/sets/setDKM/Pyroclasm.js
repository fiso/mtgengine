"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Pyroclasm extends UnimplementedCard {
  constructor (game) {
    super(game, "Pyroclasm", "Deckmasters", "DKM");
  }
}

module.exports = Pyroclasm;
