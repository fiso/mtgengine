"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DissensionintheRanks extends UnimplementedCard {
  constructor(game) {
    super(game, "Dissension in the Ranks", "Shadows over Innistrad", "SOI");
  }
}

module.exports = DissensionintheRanks;
