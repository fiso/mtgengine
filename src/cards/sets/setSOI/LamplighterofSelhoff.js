"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LamplighterofSelhoff extends UnimplementedCard {
  constructor(game) {
    super(game, "Lamplighter of Selhoff", "Shadows over Innistrad", "SOI");
  }
}

module.exports = LamplighterofSelhoff;
