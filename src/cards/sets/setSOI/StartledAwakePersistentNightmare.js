"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class StartledAwakePersistentNightmare extends UnimplementedCard {
  constructor (game) {
    super(game, "Startled Awake // Persistent Nightmare", "Shadows over Innistrad", "SOI");
  }
}

module.exports = StartledAwakePersistentNightmare;
