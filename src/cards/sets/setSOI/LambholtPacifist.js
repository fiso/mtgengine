"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LambholtPacifist extends UnimplementedCard {
  constructor (game) {
    super(game, "Lambholt Pacifist", "Shadows over Innistrad", "SOI");
  }
}

module.exports = LambholtPacifist;
