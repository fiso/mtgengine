"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SunriseSeeker extends UnimplementedCard {
  constructor (game) {
    super(game, "Sunrise Seeker", "Ixalan", "XLN");
  }
}

module.exports = SunriseSeeker;
