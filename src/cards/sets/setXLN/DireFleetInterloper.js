"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DireFleetInterloper extends UnimplementedCard {
  constructor (game) {
    super(game, "Dire Fleet Interloper", "Ixalan", "XLN");
  }
}

module.exports = DireFleetInterloper;
