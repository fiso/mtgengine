"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GrimCaptainsCall extends UnimplementedCard {
  constructor (game) {
    super(game, "Grim Captain's Call", "Ixalan", "XLN");
  }
}

module.exports = GrimCaptainsCall;
