"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RaidersWake extends UnimplementedCard {
  constructor (game) {
    super(game, "Raiders' Wake", "Ixalan", "XLN");
  }
}

module.exports = RaidersWake;
