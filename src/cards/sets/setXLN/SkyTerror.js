"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SkyTerror extends UnimplementedCard {
  constructor (game) {
    super(game, "Sky Terror", "Ixalan", "XLN");
  }
}

module.exports = SkyTerror;
