"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SirenStormtamer extends UnimplementedCard {
  constructor (game) {
    super(game, "Siren Stormtamer", "Ixalan", "XLN");
  }
}

module.exports = SirenStormtamer;
