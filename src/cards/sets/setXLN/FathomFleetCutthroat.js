"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FathomFleetCutthroat extends UnimplementedCard {
  constructor (game) {
    super(game, "Fathom Fleet Cutthroat", "Ixalan", "XLN");
  }
}

module.exports = FathomFleetCutthroat;
