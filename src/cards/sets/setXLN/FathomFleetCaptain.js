"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FathomFleetCaptain extends UnimplementedCard {
  constructor (game) {
    super(game, "Fathom Fleet Captain", "Ixalan", "XLN");
  }
}

module.exports = FathomFleetCaptain;
