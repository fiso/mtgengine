"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LoomingAltisaur extends UnimplementedCard {
  constructor (game) {
    super(game, "Looming Altisaur", "Ixalan", "XLN");
  }
}

module.exports = LoomingAltisaur;
