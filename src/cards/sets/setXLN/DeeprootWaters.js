"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeeprootWaters extends UnimplementedCard {
  constructor (game) {
    super(game, "Deeproot Waters", "Ixalan", "XLN");
  }
}

module.exports = DeeprootWaters;
