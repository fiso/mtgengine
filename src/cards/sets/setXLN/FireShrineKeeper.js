"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FireShrineKeeper extends UnimplementedCard {
  constructor (game) {
    super(game, "Fire Shrine Keeper", "Ixalan", "XLN");
  }
}

module.exports = FireShrineKeeper;
