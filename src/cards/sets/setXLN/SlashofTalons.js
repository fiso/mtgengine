"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SlashofTalons extends UnimplementedCard {
  constructor (game) {
    super(game, "Slash of Talons", "Ixalan", "XLN");
  }
}

module.exports = SlashofTalons;
