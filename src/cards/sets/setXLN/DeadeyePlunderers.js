"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeadeyePlunderers extends UnimplementedCard {
  constructor (game) {
    super(game, "Deadeye Plunderers", "Ixalan", "XLN");
  }
}

module.exports = DeadeyePlunderers;
