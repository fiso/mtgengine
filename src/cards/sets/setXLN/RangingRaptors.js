"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RangingRaptors extends UnimplementedCard {
  constructor (game) {
    super(game, "Ranging Raptors", "Ixalan", "XLN");
  }
}

module.exports = RangingRaptors;
