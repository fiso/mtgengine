"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VerdantSunsAvatar extends UnimplementedCard {
  constructor (game) {
    super(game, "Verdant Sun's Avatar", "Ixalan", "XLN");
  }
}

module.exports = VerdantSunsAvatar;
