"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RiversRebuke extends UnimplementedCard {
  constructor (game) {
    super(game, "River's Rebuke", "Ixalan", "XLN");
  }
}

module.exports = RiversRebuke;
