"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeeprootWarrior extends UnimplementedCard {
  constructor (game) {
    super(game, "Deeproot Warrior", "Ixalan", "XLN");
  }
}

module.exports = DeeprootWarrior;
