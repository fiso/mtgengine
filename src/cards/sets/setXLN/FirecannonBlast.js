"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FirecannonBlast extends UnimplementedCard {
  constructor (game) {
    super(game, "Firecannon Blast", "Ixalan", "XLN");
  }
}

module.exports = FirecannonBlast;
