"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SunBlessedMount extends UnimplementedCard {
  constructor (game) {
    super(game, "Sun-Blessed Mount", "Ixalan", "XLN");
  }
}

module.exports = SunBlessedMount;
