"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AxisofMortality extends UnimplementedCard {
  constructor (game) {
    super(game, "Axis of Mortality", "Ixalan", "XLN");
  }
}

module.exports = AxisofMortality;
