"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeathgorgeScavenger extends UnimplementedCard {
  constructor (game) {
    super(game, "Deathgorge Scavenger", "Ixalan", "XLN");
  }
}

module.exports = DeathgorgeScavenger;
