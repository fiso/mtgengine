"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TilonallisKnight extends UnimplementedCard {
  constructor (game) {
    super(game, "Tilonalli's Knight", "Ixalan", "XLN");
  }
}

module.exports = TilonallisKnight;
