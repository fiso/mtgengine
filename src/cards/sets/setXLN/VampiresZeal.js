"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VampiresZeal extends UnimplementedCard {
  constructor (game) {
    super(game, "Vampire's Zeal", "Ixalan", "XLN");
  }
}

module.exports = VampiresZeal;
