"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpindriftDrake extends UnimplementedCard {
  constructor(game) {
    super(game, "Spindrift Drake", "Stronghold", "STH");
  }
}

module.exports = SpindriftDrake;
