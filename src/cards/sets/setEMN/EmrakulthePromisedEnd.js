"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EmrakulthePromisedEnd extends UnimplementedCard {
  constructor (game) {
    super(game, "Emrakul, the Promised End", "Eldritch Moon", "EMN");
  }
}

module.exports = EmrakulthePromisedEnd;
