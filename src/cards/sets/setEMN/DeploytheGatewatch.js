"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DeploytheGatewatch extends UnimplementedCard {
  constructor (game) {
    super(game, "Deploy the Gatewatch", "Eldritch Moon", "EMN");
  }
}

module.exports = DeploytheGatewatch;
