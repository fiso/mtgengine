"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class WhispersofEmrakul extends UnimplementedCard {
  constructor (game) {
    super(game, "Whispers of Emrakul", "Eldritch Moon", "EMN");
  }
}

module.exports = WhispersofEmrakul;
