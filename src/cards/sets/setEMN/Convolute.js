"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Convolute extends UnimplementedCard {
  constructor (game) {
    super(game, "Convolute", "Eldritch Moon", "EMN");
  }
}

module.exports = Convolute;
