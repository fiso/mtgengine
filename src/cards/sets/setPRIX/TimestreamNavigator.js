"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TimestreamNavigator extends UnimplementedCard {
  constructor (game) {
    super(game, "Timestream Navigator", "Rivals of Ixalan Promos", "PRIX");
  }
}

module.exports = TimestreamNavigator;
