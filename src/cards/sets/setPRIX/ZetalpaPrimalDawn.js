"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ZetalpaPrimalDawn extends UnimplementedCard {
  constructor (game) {
    super(game, "Zetalpa, Primal Dawn", "Rivals of Ixalan Promos", "PRIX");
  }
}

module.exports = ZetalpaPrimalDawn;
