"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SinuousVermin extends UnimplementedCard {
  constructor (game) {
    super(game, "Sinuous Vermin", "You Make the Cube", "PZ2");
  }
}

module.exports = SinuousVermin;
