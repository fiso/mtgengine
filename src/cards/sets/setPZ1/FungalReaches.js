"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FungalReaches extends UnimplementedCard {
  constructor (game) {
    super(game, "Fungal Reaches", "Legendary Cube", "PZ1");
  }
}

module.exports = FungalReaches;
