"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Epochrasite extends UnimplementedCard {
  constructor (game) {
    super(game, "Epochrasite", "Commander 2014", "C14");
  }
}

module.exports = Epochrasite;
