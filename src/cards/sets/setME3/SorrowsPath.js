"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SorrowsPath extends UnimplementedCard {
  constructor (game) {
    super(game, "Sorrow's Path", "Masters Edition III", "ME3");
  }
}

module.exports = SorrowsPath;
