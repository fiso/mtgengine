"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MuzziosPreparations extends UnimplementedCard {
  constructor (game) {
    super(game, "Muzzio's Preparations", "Conspiracy", "CNS");
  }
}

module.exports = MuzziosPreparations;
