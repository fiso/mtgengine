"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GlyphofDelusion extends UnimplementedCard {
  constructor (game) {
    super(game, "Glyph of Delusion", "Legends", "LEG");
  }
}

module.exports = GlyphofDelusion;
