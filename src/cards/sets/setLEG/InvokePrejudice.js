"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class InvokePrejudice extends UnimplementedCard {
  constructor(game) {
    super(game, "Invoke Prejudice", "Legends", "LEG");
  }
}

module.exports = InvokePrejudice;
