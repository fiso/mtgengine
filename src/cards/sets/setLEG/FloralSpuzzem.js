"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FloralSpuzzem extends UnimplementedCard {
  constructor(game) {
    super(game, "Floral Spuzzem", "Legends", "LEG");
  }
}

module.exports = FloralSpuzzem;
