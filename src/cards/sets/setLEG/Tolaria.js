"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Tolaria extends UnimplementedCard {
  constructor (game) {
    super(game, "Tolaria", "Legends", "LEG");
  }
}

module.exports = Tolaria;
