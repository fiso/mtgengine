"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HarborSerpent extends UnimplementedCard {
  constructor (game) {
    super(game, "Harbor Serpent", "Magic 2011", "M11");
  }
}

module.exports = HarborSerpent;
