"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class AutumnsVeil extends UnimplementedCard {
  constructor (game) {
    super(game, "Autumn's Veil", "Magic 2011", "M11");
  }
}

module.exports = AutumnsVeil;
