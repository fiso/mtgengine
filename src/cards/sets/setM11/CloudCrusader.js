"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CloudCrusader extends UnimplementedCard {
  constructor (game) {
    super(game, "Cloud Crusader", "Magic 2011", "M11");
  }
}

module.exports = CloudCrusader;
