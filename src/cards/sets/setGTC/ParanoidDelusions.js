"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ParanoidDelusions extends UnimplementedCard {
  constructor (game) {
    super(game, "Paranoid Delusions", "Gatecrash", "GTC");
  }
}

module.exports = ParanoidDelusions;
