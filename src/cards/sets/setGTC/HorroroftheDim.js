"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HorroroftheDim extends UnimplementedCard {
  constructor (game) {
    super(game, "Horror of the Dim", "Gatecrash", "GTC");
  }
}

module.exports = HorroroftheDim;
