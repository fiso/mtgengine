"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class VelatheNightClad extends UnimplementedCard {
  constructor (game) {
    super(game, "Vela the Night-Clad", "Commander's Arsenal", "CM1");
  }
}

module.exports = VelatheNightClad;
