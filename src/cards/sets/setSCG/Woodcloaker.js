"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Woodcloaker extends UnimplementedCard {
  constructor (game) {
    super(game, "Woodcloaker", "Scourge", "SCG");
  }
}

module.exports = Woodcloaker;
