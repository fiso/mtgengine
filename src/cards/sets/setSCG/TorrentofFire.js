"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TorrentofFire extends UnimplementedCard {
  constructor (game) {
    super(game, "Torrent of Fire", "Scourge", "SCG");
  }
}

module.exports = TorrentofFire;
