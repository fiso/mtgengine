"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DivergentGrowth extends UnimplementedCard {
  constructor (game) {
    super(game, "Divergent Growth", "Scourge", "SCG");
  }
}

module.exports = DivergentGrowth;
