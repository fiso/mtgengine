"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ClawsofWirewood extends UnimplementedCard {
  constructor(game) {
    super(game, "Claws of Wirewood", "Scourge", "SCG");
  }
}

module.exports = ClawsofWirewood;
