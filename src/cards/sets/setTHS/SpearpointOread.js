"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpearpointOread extends UnimplementedCard {
  constructor(game) {
    super(game, "Spearpoint Oread", "Theros", "THS");
  }
}

module.exports = SpearpointOread;
