"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Earthbind extends UnimplementedCard {
  constructor (game) {
    super(game, "Earthbind", "Summer Magic / Edgar", "SUM");
  }
}

module.exports = Earthbind;
