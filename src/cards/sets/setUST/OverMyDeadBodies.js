"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class OverMyDeadBodies extends UnimplementedCard {
  constructor (game) {
    super(game, "Over My Dead Bodies", "Unstable", "UST");
  }
}

module.exports = OverMyDeadBodies;
