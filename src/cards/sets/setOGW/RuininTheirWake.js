"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RuininTheirWake extends UnimplementedCard {
  constructor (game) {
    super(game, "Ruin in Their Wake", "Oath of the Gatewatch", "OGW");
  }
}

module.exports = RuininTheirWake;
