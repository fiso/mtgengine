"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TajuruPathwarden extends UnimplementedCard {
  constructor (game) {
    super(game, "Tajuru Pathwarden", "Oath of the Gatewatch", "OGW");
  }
}

module.exports = TajuruPathwarden;
