"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RegnatheRedeemer extends UnimplementedCard {
  constructor (game) {
    super(game, "Regna, the Redeemer", "Battlebond Promos", "PBBD");
  }
}

module.exports = RegnatheRedeemer;
