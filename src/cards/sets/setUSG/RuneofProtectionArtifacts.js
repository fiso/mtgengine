"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RuneofProtectionArtifacts extends UnimplementedCard {
  constructor (game) {
    super(game, "Rune of Protection: Artifacts", "Urza's Saga", "USG");
  }
}

module.exports = RuneofProtectionArtifacts;
