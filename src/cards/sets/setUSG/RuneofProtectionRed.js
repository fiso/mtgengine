"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RuneofProtectionRed extends UnimplementedCard {
  constructor(game) {
    super(game, "Rune of Protection: Red", "Urza's Saga", "USG");
  }
}

module.exports = RuneofProtectionRed;
