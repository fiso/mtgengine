"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FlamerushRider extends UnimplementedCard {
  constructor (game) {
    super(game, "Flamerush Rider", "Fate Reforged", "FRF");
  }
}

module.exports = FlamerushRider;
