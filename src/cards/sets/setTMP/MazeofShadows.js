"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MazeofShadows extends UnimplementedCard {
  constructor (game) {
    super(game, "Maze of Shadows", "Tempest", "TMP");
  }
}

module.exports = MazeofShadows;
