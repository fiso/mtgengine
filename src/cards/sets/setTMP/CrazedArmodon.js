"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CrazedArmodon extends UnimplementedCard {
  constructor (game) {
    super(game, "Crazed Armodon", "Tempest", "TMP");
  }
}

module.exports = CrazedArmodon;
