"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CircleofProtectionShadow extends UnimplementedCard {
  constructor(game) {
    super(game, "Circle of Protection: Shadow", "Tempest", "TMP");
  }
}

module.exports = CircleofProtectionShadow;
