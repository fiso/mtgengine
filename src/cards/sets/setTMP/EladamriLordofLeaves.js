"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EladamriLordofLeaves extends UnimplementedCard {
  constructor (game) {
    super(game, "Eladamri, Lord of Leaves", "Tempest", "TMP");
  }
}

module.exports = EladamriLordofLeaves;
