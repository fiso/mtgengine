"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Skullscorch extends UnimplementedCard {
  constructor (game) {
    super(game, "Skullscorch", "Torment", "TOR");
  }
}

module.exports = Skullscorch;
