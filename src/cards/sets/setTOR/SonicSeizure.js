"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SonicSeizure extends UnimplementedCard {
  constructor (game) {
    super(game, "Sonic Seizure", "Torment", "TOR");
  }
}

module.exports = SonicSeizure;
