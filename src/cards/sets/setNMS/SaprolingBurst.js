"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SaprolingBurst extends UnimplementedCard {
  constructor (game) {
    super(game, "Saproling Burst", "Nemesis", "NMS");
  }
}

module.exports = SaprolingBurst;
