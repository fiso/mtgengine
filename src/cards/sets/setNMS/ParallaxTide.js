"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ParallaxTide extends UnimplementedCard {
  constructor (game) {
    super(game, "Parallax Tide", "Nemesis", "NMS");
  }
}

module.exports = ParallaxTide;
