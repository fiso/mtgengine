"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class PhoberosReaver extends UnimplementedCard {
  constructor (game) {
    super(game, "Phoberos Reaver", "Battle the Horde", "TBTH");
  }
}

module.exports = PhoberosReaver;
