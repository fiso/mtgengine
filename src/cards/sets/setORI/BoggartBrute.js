"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BoggartBrute extends UnimplementedCard {
  constructor (game) {
    super(game, "Boggart Brute", "Magic Origins", "ORI");
  }
}

module.exports = BoggartBrute;
