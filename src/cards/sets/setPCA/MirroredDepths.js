"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MirroredDepths extends UnimplementedCard {
  constructor (game) {
    super(game, "Mirrored Depths", "Planechase Anthology", "PCA");
  }
}

module.exports = MirroredDepths;
