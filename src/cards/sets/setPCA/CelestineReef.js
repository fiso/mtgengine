"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CelestineReef extends UnimplementedCard {
  constructor (game) {
    super(game, "Celestine Reef", "Planechase Anthology", "PCA");
  }
}

module.exports = CelestineReef;
