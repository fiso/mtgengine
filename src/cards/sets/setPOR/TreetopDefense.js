"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TreetopDefense extends UnimplementedCard {
  constructor (game) {
    super(game, "Treetop Defense", "Portal", "POR");
  }
}

module.exports = TreetopDefense;
