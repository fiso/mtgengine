"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ParallaxTide extends UnimplementedCard {
  constructor (game) {
    super(game, "Parallax Tide", "World Championship Decks 2000", "WC00");
  }
}

module.exports = ParallaxTide;
