"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Erosion extends UnimplementedCard {
  constructor(game) {
    super(game, "Erosion", "Fourth Edition", "4ED");
  }
}

module.exports = Erosion;
