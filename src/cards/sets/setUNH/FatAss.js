"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class FatAss extends UnimplementedCard {
  constructor (game) {
    super(game, "Fat Ass", "Unhinged", "UNH");
  }
}

module.exports = FatAss;
