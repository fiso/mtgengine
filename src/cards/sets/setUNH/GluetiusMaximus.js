"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class GluetiusMaximus extends UnimplementedCard {
  constructor (game) {
    super(game, "Gluetius Maximus", "Unhinged", "UNH");
  }
}

module.exports = GluetiusMaximus;
