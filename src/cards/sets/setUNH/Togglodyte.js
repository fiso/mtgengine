"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Togglodyte extends UnimplementedCard {
  constructor (game) {
    super(game, "Togglodyte", "Unhinged", "UNH");
  }
}

module.exports = Togglodyte;
