"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SixyBeast extends UnimplementedCard {
  constructor (game) {
    super(game, "Six-y Beast", "Unhinged", "UNH");
  }
}

module.exports = SixyBeast;
