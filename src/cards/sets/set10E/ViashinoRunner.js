"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class ViashinoRunner extends UnimplementedCard {
  constructor(game) {
    super(game, "Viashino Runner", "Tenth Edition", "10E");
  }
}

module.exports = ViashinoRunner;
