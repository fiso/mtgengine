"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Luminesce extends UnimplementedCard {
  constructor (game) {
    super(game, "Luminesce", "Coldsnap", "CSP");
  }
}

module.exports = Luminesce;
