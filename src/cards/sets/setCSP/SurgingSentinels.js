"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SurgingSentinels extends UnimplementedCard {
  constructor (game) {
    super(game, "Surging Sentinels", "Coldsnap", "CSP");
  }
}

module.exports = SurgingSentinels;
