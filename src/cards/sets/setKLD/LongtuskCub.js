"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LongtuskCub extends UnimplementedCard {
  constructor (game) {
    super(game, "Longtusk Cub", "Kaladesh", "KLD");
  }
}

module.exports = LongtuskCub;
