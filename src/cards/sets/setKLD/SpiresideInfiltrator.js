"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpiresideInfiltrator extends UnimplementedCard {
  constructor (game) {
    super(game, "Spireside Infiltrator", "Kaladesh", "KLD");
  }
}

module.exports = SpiresideInfiltrator;
