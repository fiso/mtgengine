"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class DhundOperative extends UnimplementedCard {
  constructor (game) {
    super(game, "Dhund Operative", "Kaladesh", "KLD");
  }
}

module.exports = DhundOperative;
