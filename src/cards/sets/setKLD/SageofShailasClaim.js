"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SageofShailasClaim extends UnimplementedCard {
  constructor (game) {
    super(game, "Sage of Shaila's Claim", "Kaladesh", "KLD");
  }
}

module.exports = SageofShailasClaim;
