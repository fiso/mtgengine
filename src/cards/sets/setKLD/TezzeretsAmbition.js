"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class TezzeretsAmbition extends UnimplementedCard {
  constructor (game) {
    super(game, "Tezzeret's Ambition", "Kaladesh", "KLD");
  }
}

module.exports = TezzeretsAmbition;
