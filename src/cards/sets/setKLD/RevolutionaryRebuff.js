"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RevolutionaryRebuff extends UnimplementedCard {
  constructor (game) {
    super(game, "Revolutionary Rebuff", "Kaladesh", "KLD");
  }
}

module.exports = RevolutionaryRebuff;
