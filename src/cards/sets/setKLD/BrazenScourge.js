"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class BrazenScourge extends UnimplementedCard {
  constructor (game) {
    super(game, "Brazen Scourge", "Kaladesh", "KLD");
  }
}

module.exports = BrazenScourge;
