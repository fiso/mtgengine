"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class EddytrailHawk extends UnimplementedCard {
  constructor (game) {
    super(game, "Eddytrail Hawk", "Kaladesh", "KLD");
  }
}

module.exports = EddytrailHawk;
