"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class MetallurgicSummonings extends UnimplementedCard {
  constructor (game) {
    super(game, "Metallurgic Summonings", "Kaladesh", "KLD");
  }
}

module.exports = MetallurgicSummonings;
