"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class RiparianTiger extends UnimplementedCard {
  constructor (game) {
    super(game, "Riparian Tiger", "Kaladesh", "KLD");
  }
}

module.exports = RiparianTiger;
