"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class HightideHermit extends UnimplementedCard {
  constructor (game) {
    super(game, "Hightide Hermit", "Kaladesh", "KLD");
  }
}

module.exports = HightideHermit;
