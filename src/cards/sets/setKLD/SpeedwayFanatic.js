"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SpeedwayFanatic extends UnimplementedCard {
  constructor (game) {
    super(game, "Speedway Fanatic", "Kaladesh", "KLD");
  }
}

module.exports = SpeedwayFanatic;
