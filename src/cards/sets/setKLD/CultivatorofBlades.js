"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class CultivatorofBlades extends UnimplementedCard {
  constructor (game) {
    super(game, "Cultivator of Blades", "Kaladesh", "KLD");
  }
}

module.exports = CultivatorofBlades;
