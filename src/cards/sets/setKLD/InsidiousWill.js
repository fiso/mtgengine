"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class InsidiousWill extends UnimplementedCard {
  constructor (game) {
    super(game, "Insidious Will", "Kaladesh", "KLD");
  }
}

module.exports = InsidiousWill;
