"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class LongFinnedSkywhale extends UnimplementedCard {
  constructor (game) {
    super(game, "Long-Finned Skywhale", "Kaladesh", "KLD");
  }
}

module.exports = LongFinnedSkywhale;
