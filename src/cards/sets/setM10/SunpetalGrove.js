"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class SunpetalGrove extends UnimplementedCard {
  constructor (game) {
    super(game, "Sunpetal Grove", "Magic 2010", "M10");
  }
}

module.exports = SunpetalGrove;
