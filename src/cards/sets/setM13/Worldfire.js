"use strict";
const Constants = require ("../../../Constants");
const UnimplementedCard = require("../../UnimplementedCard");

class Worldfire extends UnimplementedCard {
  constructor (game) {
    super(game, "Worldfire", "Magic 2013", "M13");
  }
}

module.exports = Worldfire;
